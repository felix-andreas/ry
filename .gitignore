.direnv
result

out
node_modules
*.tsbuildinfo

.vscode-test
*.vsix

/target
/release

# Coverage profiles, written into the crate directory the instrumented
# binary ran from.
*.profraw
*.profdata

.local
.rlib
r-examples
# Local R installations at the repo root only. Unanchored `R*/` would match
# ANY directory starting with `r` on case-insensitive filesystems (Windows,
# macOS) — including `crates/ry/`, silently dropping new files there.
/R*/
ry.toml
roughly.toml

# Claude Code local harness state (config, worktrees)
.claude/

# Fetched parser corpus (populate with scripts/fetch-corpus.rs; pinned inventory
# lives in scripts/corpus-manifest.txt).
/corpus/
