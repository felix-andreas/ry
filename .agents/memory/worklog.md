# Work log

This file holds one line per autonomous work cycle, newest last.

It is the one file in `.agents/memory/` that is deliberately chronological. The timeless rule that
governs `MEMORY.md`, `backlog.md` and `decisions.md` does not apply here, so do not turn it into a
knowledge document. A durable fact still belongs in those files. This file only records what
happened, so a later session can see the shape of recent work without re-reading the git log.

Write an entry as `YYYY-MM-DD HH:MM: what landed. Loose end, if any.`

Keep an entry to one line. A cycle that found nothing worth doing says so, because an empty line is
more useful than invented work.

Entries written before the project's writing rules landed keep their original wording. They are a
record of what happened, so they are not rewritten.

---

- 2026-07-26 22:05 — Overload selection reduced to plain first-match (removed the last-fitting tiebreak, which conflicted with the value-use rule); `lapply`/`Filter`/`rev`/`unique`/`head`/`tail` now preserve input shape and names; all-fail diagnostics report the deepest candidate instead of the wrapper; signature help no longer needs a committed candidate. Recorded the HM-only bar and declined traits. Rewrote the inline-typing proposal three times against two adversarial reviews, which caught a contract reversal and three false motivating claims. Naming decided: `.ry` in `Ry/`, `base.ry.stub`, CLI rename to `ry` recommended but unsettled. Loose end: the silently-dropped annotation in a non-attachable position is filed but unfixed.
- 2026-07-26 23:10 — Renamed the language and toolchain to `ry`: crate `crates/ry` published as `ry-lang`, binary and lib `ry`, docs/editors/CI/README swept, docs pointed at ry-lang.org, README carries the shorter-name joke with verified parser output. Former spellings all still work — `roughly.toml`, `# roughly: allow(...)`, `ROUGHLY_*`, the extension's `roughly.*` settings — and the REPL history directory migrates itself. Loose ends for the user: repository rename, Marketplace identifier, crates.io registration, and the hero animation, which still spells the old name and is user-owned.
- 2026-07-27 01:05 — Fixed the `rlang` crash: `scc_schemes` was the only query in the interface-fixpoint chain without salsa cycle recovery, so a reference edge the static graph cannot see aborted the process; recovery now pins the group to `Unknown` and refuses on first disagreement rather than iterating (the iterating version measured *worse* than the bug — it turned the panic into an OOM kill). Verified: rlang's `R/` went exit 101 → clean run, 838 findings. Reviewed PR #89 (miette renderer) with feedback rather than merging — 161 tests verified green, no CI ran on it, and it needs the hero SVG regenerated since it changes diagnostic rendering. Also fixed the `cargo install` spec the rename broke. Loose ends: checking rlang's *whole* package now OOMs (the panic was masking it), the cycle fix has no fixture because a synthetic repro would not reproduce it, and `fmt --check` costs twice what `check` does.
- 2026-07-27 02:25 — Rebuilt the README against two adversarial reviews, which turned into two real bug fixes. `typing = true` written outside `[check]` had loaded clean and checked nothing — the warning now names the table. Backtick-quoted names ignored R's escape rules, so `` `foo}\`` `` ended early and cascaded (59 false syntax errors in one `glue` file); lexer and `Name::text` fixed against real R, and the tree-sitter fixture that had blessed the wrong parse re-blessed. Corrected four false claims in the type-system prose plus the normative spec, which said `Unknown` is incompatible with concrete types when it is compatible with everything. Everything the README asserts was re-verified by running it: perf table re-measured (~50k lines/s, flat across 3.2×), edit-to-diagnostics measured at ~63 ms on 323k lines, 69 CRAN packages swept (66 finish). The earlier "rlang OOMs" note was wrong and is corrected in the backlog: it is a fixpoint that never settles. Loose ends: `htmltools`/`mgcv` spin at flat memory (a separate pathology), a package does not recognize its own namespace, and an ignored config key is still only a warning.
- 2026-07-27 03:45 — A package now resolves names qualified with its own namespace: `withr::defer()` inside `withr` had reported `unknown package namespace`, a false positive on the likeliest input there is, and now reads the project's own definition and carries its type instead of `Unknown` (the own package also beats a stub namespace of the same name). Validating the *name* was built, measured across the CRAN corpus, and deleted — all sixteen candidate reports were false positives, because a package exports names its sources never bind (re-export, `setGeneric`, `data/` dataset, `.onLoad` binding, re-exported S3 generic); filed with the five mechanisms. Four fixtures, typing reference updated with both halves. PR #89 unchanged since its review, so nothing to do there. Loose end: a typo in a self-qualified call is still invisible, and closing it needs a real export-set model including re-export resolution and an `exportPattern` decision.
- 2026-07-27 07:05 — Fixed the second `rlang` cycle failure: `item_check_recover` pinned only `scheme` at the round cap and took `ItemCheck`'s six other fields from the just-recomputed value, so no round ever compared equal and salsa ran to its `MAX_ITERATIONS` of 200 before panicking (or exhausting memory first — one bug, two symptoms). The refusal now re-pins what was already returned, a fixed point by construction, and cuts both export surfaces; the sibling recoveries were safe only because a bare `TypeScheme` pin is already constant. Whole-package rlang: 213-second death → clean 9-second run, 806 findings; the flattened variant that OOM-killed now takes 14 s. Also closed the "overload corpus is inflated by missing grammar" item as a false premise — constrained binders already work everywhere, and the extra candidates encode R's logical→integer promotion rather than a grammar gap. Loose ends: `htmltools`/`mgcv` still spin at flat memory (confirmed separate — unchanged by this fix, no cycle panic), no end-to-end fixture for either cycle bug (only the structural idempotence property is pinned), and a stdlib wrapper still loses its types (`function(x) abs(x)` is `fn(x: T) -> Any`), newly filed with the constraint any fix must respect.
- 2026-07-27 09:35 — Fixed the worst finding from the typing-enthusiast round: a field write through a nominal value was neither checked nor tracked (`replacement_written_type` fell through to `_ => prior`), so the checker kept the declared field type and answered from it both ways — accepting a call that crashes and rejecting one that is correct, silently, even under strict. The write is now checked against the fixed representation and reported at the value, the binding keeps its nominal type, and `@type` finally holds past construction; structural records still retype, pinned as the contrast. Seven fixtures, reference updated. Also ran the round itself (three testers on the type system rather than libraries, location and message judged separately) and filed all of it before fixing any. Loose ends: the second wrong-answer bug is now top of the list (a function-valued `if` without `else` keeps only the last branch — R returns 2 for the repro, ry rejects it), top-level guard narrowing still does not fire though the reference promises it, and a structural field write is lost across items in a package but not in a script.
- 2026-07-27 12:45 — Closed the second wrong-answer bug from the typing round: a branch join involving a function slot kept one arm, so a conditionally reassigned function exported a signature belonging to neither path (parameter from one branch, return from the other) — accepting a character use where R gives integer and refusing numeric use with a false `found character`. Joining the monotypes was half the fix; `join_types` unifies before unioning and two *instantiated* schemes always unify through their fresh variables, which is precisely how the hybrid arose, so a cross-path join now unions and never unifies. Five fixtures including the let-polymorphism guard; the reference already specified this join, so only the union-not-unify clarification was added. Corrected the report's framing in the backlog — R fails on the other path, so refusing the repro is right and the bug was the invented type, not the refusal. Also nearly filed a non-bug: a union-of-functions call looked broken but was my own annotation's `->`/`|` precedence. Loose ends: top-level guard narrowing still does not fire though the reference promises it (next), a structural field write is lost across items in a package but not a script, and `htmltools`/`mgcv` still spin at flat memory.
- 2026-07-27 15:50 — Guard narrowing now fires at file top level: `recognize_guard` required the tested read to be in `naming.resolutions`, and a top-level variable read by a later statement is a non-local with no slot to refine, so the guard bailed — which is why the identical guard in a function body worked and hid it. A non-local read now gets a checker-minted slot (ids above naming's range) seeded with the observed type, and the environment carries the refinement, so flow state keeps one home. Null, negated and family guards all narrow; five script fixtures. Instrumented the bail rather than guessing at it. Verified two things that changed the outcome: narrowing into a closure is what locals already do (not a new unsoundness), and the early-return idiom across bare top-level statements cannot work because flow state does not cross items — documented in the reference and pinned by a fixture instead of hidden, since the reference had promised "script locals narrow" flatly. Loose ends: a duplicate `@type`/`@alias` name is still unchecked in script projects (silently wins, next), a structural field write is lost across items in a package but not a script, and `htmltools`/`mgcv` still spin at flat memory.
- 2026-07-27 18:30 — A type name declared twice inside one script is now reported. The filed finding's premise was half wrong and checking it first is what stopped a false positive: a script's type declarations reach only their own file (`@alias Shared` in `one.R` is invisible from `two.R`), so two scripts may each declare `Thing` and flagging that would have broken correct code. The real gap was the repeat *within* one file, where the namespace is shared — previously silent, so the later declaration won and its mismatch was unfalsifiable from the visible source. Both sites now report, each pointing at the other, worded for the namespace actually being judged; the package path is untouched since its project map already counted one file per declaration. Three script fixtures plus a CLI test for the cross-file no-conflict half, which a one-file fixture case cannot express. Corrected both reference pages, which said "regardless of file" and "anywhere in the project". Loose ends: `= NULL` is exempt from the default-value check (next), a structural field write is lost across items in a package but not a script, and `htmltools`/`mgcv` still spin at flat memory.
- 2026-07-27 21:30 — A `NULL` default is now checked against the declared parameter type. The exemption was a documented decision, not an oversight, so it was measured rather than flipped: R tolerates NULL in most character positions (`nchar(NULL)` is `integer(0)`) but not in a comparison, so `[title]: character` with `title = NULL` let `if (title == "draft")` pass the checker and fail at run time with `argument is of length zero` — while `character | NULL` catches it statically and an `is.null()` guard clears it, so the remedy already worked and only the exemption blocked it. Own diagnostic rather than a bare mismatch, naming the nullable spelling and the guard; `Any` still admits NULL, unannotated parameters unaffected, non-NULL defaults unchanged. Three reference examples relied on the exemption — one declared `[label] {character}` then guarded with `!is.null(label)`, the spec demonstrating the lie in its own sample — and now read `character | NULL`. Six fixtures including the crash the lying annotation hid. Loose ends: an annotation in a position it cannot attach to is still silently dropped (next), a structural field write is lost across items in a package but not a script, and `htmltools`/`mgcv` still spin at flat memory.
- 2026-07-28 01:00 — A `#:` annotation inside a call's argument list is now reported; it attached to nothing and said nothing, so a wrong type beside a lambda argument was invisible (`statement_annotations` sees an `ARGUMENT` node next, which is not an expression kind, and the placement walk never visits an argument list). Scope was much narrower than the filed item and took two wrong attempts: reporting every annotation the walk missed is a false positive on four positions that do attach, built and reverted; then an uncontrolled probe read `lapply(xs, 1L)`'s own "not a function" error as the annotation applying, which a no-annotation control disproved. Verified per position — braceless body, braceless `if` branch and parens all attach; only `ARGUMENT_LIST` drops — so the check keys on that alone, with two fixtures pinning reports and two pinning the attaching positions. Rebased onto seven commits the user pushed mid-cycle (repo rename, README and landing-page rewrite, docs rework #90) and re-ran the gate against them; the user's rename supersedes the previous cycle's link commit, which had pointed URLs back at `roughly` when the remote still was. Loose ends: a structural field write is lost across items in a package but not a script, `htmltools`/`mgcv` still spin at flat memory, and a lambda parameter still has no annotatable position.
- 2026-07-28 03:35 — Fixed the cross-item field write, carried as a loose end for three cycles: `SalsaGlobals::scheme` consulted a position-aware lookup built for scripts only, then the project-wide definition map, so in a package a later top-level `record$age <- …` was invisible and the next read answered from the pre-write type (the export was fine all along; `conditional_slot_items` already held the writer and nothing asked it). The ordered item list is now built for both kinds, and the composition constraint resolves by splitting on read kind rather than document kind — a deferred read stands aside for a package so a later file's override still wins and forward references keep resolving. Three fixtures; rule recorded in MEMORY.md. Also reviewed PR #89 with measured facts rather than an estimate: `git merge-tree` shows 16 conflicting files, ten of them docs pages it re-captured that #90 has since rewritten. Deliberately did NOT sweep diagnostic wording — measured 62 messages (36 lowercase/no period, 14 lowercase/period, 9 capitalised) and the filed "lowercase fragments" rule does not survive, since the capitalised ones are `R's $ operator` and `I could not resolve …`; sweeping now would also invalidate #89's captures. Loose ends: `htmltools`/`mgcv` still spin at flat memory, the wording sweep is blocked on #89, and a lambda parameter still has no annotatable position.
- 2026-07-28 07:30 — Localised and profiled the `htmltools` hang but did not fix it, and reverted two measured non-fixes rather than ship them. One file does it: `tag_query.R` (1,563 lines) alone exceeds 25 s while every other htmltools file finishes under 900 ms; its `tagQuery_` returns `structure(list(...))` whose ~40 fields are closures each calling the enclosing closure again, so the record type is self-referential through its own fields. A symbolised sample (the repo's `profiling` profile, since release is stripped) puts the time in `substitute_rigid` recursing into salsa interning. Both candidate fixes — early-out on an empty substitution, and a memoised per-interned-type rigid-name set skipping disjoint subtrees — left the hanging file unchanged and showed zero difference on 323K lines interleaved, which is the finding: the walk is doing real work on a type that should never have grown that large, so the fix is recursion widening upstream, not substitution. Recorded a measurement trap that cost real time: a non-interleaved A/B read 9.6 s vs 4.8 s, an apparent 2× win that was entirely load drift; interleaved run-by-run the difference was zero. PR #89 unchanged (same head SHA) and already reviewed twice, so nothing new to say there. Loose ends: the hang itself is open with the diagnosis attached, `mgcv` presumably shares the shape, and nothing caps how large one constructed type may get.
- 2026-07-28 10:10 — Memoised `substitute_rigid`: interning makes a type a DAG, so a record whose ~40 fields each return that record is revisited once per field per level and an unmemoised walk is exponential in depth (no depth cap can help — the cap bounds depth, the cost is paths). Per-node memo for one top-level call, sound because the substitution is fixed for its duration. Interleaved: wins every round on 323k lines and on MASS by ~2%, identical findings, and 278M calls → under 2M on the pathological file. Does NOT fix the `htmltools` hang — a second bottleneck now dominates, still open. The correction matters more than the patch: last cycle's "substitute_rigid is not the hot path" rested on three probe counters printing nothing, and they were printing — stderr through a pipe is lost when `timeout` kills the process, so "never called" and "output discarded" read identically. Redirecting to a file showed 278M immediately. That trap, the interleave-always rule (a sequential A/B fabricated a 2x win, a cold first run a 12% one), and the DAG-walk rule are all in MEMORY.md now. Loose ends: the hang itself, `mgcv` presumably sharing the shape, and no size bound on constructed types.
- 2026-07-28 12:05 — Measured the type explosion behind the `htmltools` hang but did not fix it; no code shipped, and the growth cap I tried was reverted. Instrumenting the captured-write join to report *tree* size (paths, not distinct nodes) shows 877 → 8823 → 104655 → 1046623 → past a 5M ceiling, roughly ×10 per join, so the type genuinely explodes and every walk over it is a symptom — which is why memoising one walk only hands the hot spot to the next (`substitute_rigid` fixed last cycle, `erase_vars` became hot immediately after). Six candidate fixes are now measured and recorded as dead ends; the last is the informative one: capping per-slot join growth does nothing because each of the ~40 method slots grows once or twice and the explosion is the *product across* slots, not iteration within one, so no per-slot counter can see it. What remains is a size bound where types are built, and computing it needs a memoised `size(node) = 1 + sum(children)` because the DAG is small while the tree is huge — a distinct-node count is blind to this and a naive tree count is itself exponential. PR #89 unchanged (same head SHA), reviewed twice, nothing new. Loose ends: the hang, `mgcv` presumably sharing the shape, and no size bound on constructed types.
- 2026-07-28 14:10 — Fixed the `htmltools` hang, open across four cycles: a type is now refused once it has grown past what anything can consume. `type_size` is a tracked query counting the type **as a tree** (following every path, saturating at 100,000) — tree size, not distinct-node count, is the whole point, because interning keeps the stored graph small while every consumer pays the tree. `Checker::record` is the site: every expression's inferred type passes through it, so bounding composites there covers all construction, whereas the captured-write join where the growth was first measured fires only nine times and capping it changed nothing. Whole package >5 min → 129 ms, `tag_query.R` >200 s → 56 ms, findings byte-identical across 1,951 files (p18 16087/1531, MASS 209/71, ggplot2 1318/349), and ~7% faster interleaved on 323k lines winning every round. The guard is a unit test rather than a fixture: a self-referential case small enough to read in a fixture converges on its own without reaching the ceiling, so a blessed fixture would pin a huge expectation that proves nothing — dropped one for that reason. Correction to this item's own title: `mgcv` is NOT the same bug, it still exceeds 200 s with the ceiling in place, so it needs the per-file timing sweep that localised htmltools rather than an assumed shared cause. Loose ends: `mgcv`, the diagnostic wording sweep still blocked on PR #89 (unchanged, same head SHA), and a lambda parameter still has no annotatable position.
- 2026-07-28 18:55 — Fixed the `@param` cascade, and reproducing it showed the filed bug was one symptom of a general defect: an annotation was declaring the parameter **list**, not just the parameter types, so the exported signature was the annotation as written and every arity disagreement got resolved in the annotation's favour and charged to the call sites. Five shapes, all measured: a phantom `@param` (the filed one, 1 real error + 1 per call), **annotating only some parameters** — the ordinary way to start, and it reported every correct call while saying nothing at the definition — a surplus declared type (same), and two going the other way that *accepted* call shapes R rejects (`[x]` declared optional on a formal without a default, and a declared `...` on a fixed function; both confirmed against R 4.3.3 rather than assumed). `check_declared_function` now builds the signature from the formals — names, order, optionality, `...` position — fills types from the declaration name-aware, and reports each disagreement once at the definition. Two things had to come with it for the signature to be honest: an undeclared formal keeps its inferred type (so partial annotation now *adds* checking — `f(1L, "no")` is caught where the whole call used to be), and an elided return is inferred from the body, which the reference already promised and the implementation never did — a fixture literally named `elided_definition_return_still_infers` was blessed at `-> Unknown`. Treated a written `-> Unknown` the same way, on the reference's own wording that `Unknown` records "the checker could not tell" and is "not an explicit escape hatch" while `Any` is the way to say do-not-check. Trap worth keeping: `table.resolve` expands aliases by design, so resolving a declared signature turned `fn(x: UserId) -> UserId` into `fn(x: integer) -> integer` — resolve only the variables you introduced. Also deleted `reconcile_declared_optionality`, a second partial version of this reconciliation reachable only from the item root, so nested definitions never got it. Seven fixtures, gate green at 70 suites, findings byte-identical across data.table/dplyr/ggplot2/shiny (7,116 findings — no `#:` in those, so a no-collateral check rather than coverage). Loose ends: `mgcv` still exceeds 200 s and is a different cause from the htmltools one, the wording sweep is still blocked on PR #89 (unchanged, same head SHA, already reviewed twice), and a lambda parameter still has no annotatable position.
- 2026-07-28 22:05 — A rejected function value now names the position that failed instead of printing both signatures: `lapply(words, function(s) s + 1L)` over a character list said ``expected `fn(character) -> T`, found `fn(s: U) -> U` ``, which describes a call that should have fit. The filed premise ("binder constraints are dropped from every rendered type") held — confirmed directly, `function(x) x + 1L` and `function(x) x` print identically in a finding — but the diagnosis pointed at the renderer and that is not fixable there: a constraint belongs to the *variable*, can only appear in a binder prefix, and a diagnostic renders a monotype, so `fn(s: U) -> U` has nowhere to put "U must be numeric". Two whole signatures are simply the wrong shape for this failure. `explain_function_mismatch` re-walks the pairing and reports the one position that failed, saying what it needs rather than showing a type it cannot show — *this function is passed `character`, but its parameter `s` is used as a numeric value*, and *this function must return `logical`, but its body produces a numeric value* for the return case (`Filter`, which fails on the return not a parameter, and which the first cut missed). Both signatures still print for a shape disagreement — arity, optionality, rest parameter — the case they do explain, with a fixture pinning that fallback. Pairing is now one shared function so the verdict and the explanation cannot drift. Deliberately did NOT take the filed alternative (push the expected parameter type into the lambda body): `CallArgument` infers each argument exactly once before any signature matching so an overload probe can re-match cheaply, and bidirectional lambda checking would re-infer the body per candidate — recorded in MEMORY.md as the constraint, and worth its own slice only if callback findings need to point *inside* the lambda. Four fixtures, gate green at 70 suites; across data.table/dplyr/ggplot2/shiny the finding counts are unchanged and exactly two messages differ, both real findings that now read clearly. Loose ends: `mgcv`, the wording sweep still blocked on PR #89 (unchanged again, same head SHA), and a lambda parameter still has no annotatable position.
- 2026-07-29 00:40 — A function member of a union now renders with its grouping parentheses, so `(fn(A) -> B) | NULL` and `fn(A) -> (B | NULL)` stop printing as the same string: `->` extends over a whole union, so those are different types and a type copied out of a finding into an annotation silently changed meaning. The reference already specified the fixed behaviour ("which is also the form such a union renders as"); the renderer just joined members with ` | `. One arm in the union renderer, not a precedence framework — a function inside a union is the *only* ambiguity in the rendered grammar, since every other nesting is closed by a bracket, comma or paren. It reached past the filed case: the nullable-callback finding said "this may be `NULL` here … its type is `fn(x: Any) -> character | NULL`", describing a function returning a nullable character while talking about a value that may itself be NULL, and a branch join printed `fn(shout: logical) -> fn(x: T) -> T | fn(x: U) -> character`, which under the `->`-extends rule is not the type meant. One fixture pins both spellings written as the rendered forms pasted back, so it fails if either stops round-tripping. Counts unchanged across data.table/dplyr/ggplot2/shiny; 71 messages now parenthesize, including a shiny union that read as six members and is four. The other half of the cycle was a correction rather than a fix: the backlog's "logical is rejected at an inferred numeric parameter — smaller, but cheap" is not cheap and the current refusal is the SOUND choice. Both halves reproduce and R does promote (`TRUE + 1L` is `2L`), but binding the numeric variable to `integer` — the obvious repair — gives the wrong return type for `function(x) { stopifnot(x + 1L > 0L); x }`, which R returns as `logical` while `function(x) x + 1L` returns `integer`; one binding of `T` cannot be both, and a wrong type ranks below a refusal here. Verified against R 4.3.3 and recorded with the counter-example, along with a false positive found while measuring it: `add <- function(a, b) a + b` ties both operands to one variable, so `add(1L, 1.5)` is rejected though R gives 2.5. Both need a numeric join in the type language and should be designed together. Loose ends: that join, `mgcv`, the wording sweep still blocked on PR #89 (unchanged again), and a lambda parameter still has no annotatable position.
- 2026-07-29 04:35 — A refused `#:` annotation now reports once, and the report is the true one. The filed case gave five findings whose *first* was false — "only one compact annotation fits in a `#:` block" against a block holding exactly one, prescribing a blank line that would not have helped — and the braceless `@param count integer` claimed the same clash. Three separate causes, all closed: the form classifier counted parser-recovery debris as block items (recovery re-parents the pieces of an unreadable type to the block, so one line yielded three top-level types — the rules are about *lines*, since every one of them says "separate with a blank line", so whole `#:` lines are compared now and a line with a second item means "this line did not parse", never "the forms clash"); the refusal recovered by consuming the binder and stopping, leaving the position with no type so the enclosing parameter list failed on what followed, which now reads the type as if the binder were absent and incidentally gives the annotation a correctly nested tree instead of three sibling fragments; and a refused block still carried its typing payload, so the binder's names reported as unknown types on top of the refusal — the parser marks a refused region with an `ERROR` node and such a block carries no payload, which the contract already required for shape violations. Every rank-2 position is one finding now: parameter, return, list element, and directive payload — that last was **silently accepted** before, since `ann_braced_type` allowed binders, so `@param f {<T> fn(T) -> T}` parsed, bound nothing and said nothing. Also moved the code: it names whose grammar broke, not which stage noticed, so annotation-grammar errors are `annotation` rather than `syntax-error` (`SyntaxError::in_annotation` already carried the split, and the siblings were only `annotation` because lowering, not parsing, caught them); `diagnostic-codes.md` documented the old rule and is updated. Two traps found by breaking the suite and recorded: an `ANNOTATION_MARKER` is NOT reliably a direct child of the block — a stitched line's marker lands inside whatever node was open across the break, so line boundaries must be counted over all tokens — and a `<T>` binder list is a *prefix* of the type after it, not an item, so counting it as one refused every stub declaration with a binder and silently emptied the entire stdlib corpus. Five fixtures, gate green at 70 suites, findings byte-identical across data.table/dplyr/ggplot2/shiny. Loose ends: the same cascade shape is still visible for `fn([x] integer)` (4 findings) and `fn([1]: integer)` (9) — ordinary recovery rather than a deliberate refusal, so it needs resynchronising at the parameter boundary; plus `mgcv`, the numeric-join design, the wording sweep still blocked on PR #89 (unchanged again), and a lambda parameter still has no annotatable position.
- 2026-07-29 07:15 — Four caret-placement failures fixed under one rule now in the reference (§Where a finding points): a finding underlines the smallest expression its message is about. The trap is structural — a check naturally holds the *enclosing* range, so the default caret spans text containing both types the message contrasts, which makes the message unreadable. (1) A cross-family comparison underlined the whole binary expression and now blames the right operand, the `found` half of `expected …, found …`, where arithmetic already pointed; the behaviour itself is deliberate and stays, since R really does coerce across atomic families (`10L < "9"` is TRUE, comparing `"10" < "9"` as text — checked against R 4.3.3) and the reference documents the same-family rule as sound-by-refusal with that exact rationale, so only the caret was wrong. (2) `$`/`[[` underlined the entire access chain rather than the bad key even while naming it and suggesting a correction; both now point at the key, position access included — `ExpressionKind::Field` gained a `name_range`, the same shape `CallArgument::name_range` already used for the same reason, because a token's range is not derivable from its parent's without re-parsing. (3) A surplus positional argument blamed the callee and now blames the first argument with no formal left to take it; a *missing* argument still blames the callee, since there is no argument to point at. (4) A declared return with an `if`/`else` tail blamed the whole construct and the offending arm was never rendered at all, because a multi-line range clamps to its first line — the return is now checked against each expression that can produce the result (block tail, both `if` arms) with each failure at its own site, the rule explicit `return`s already followed. Kept the whole body's type as the verdict and used the leaves only for placement, deliberately: checking leaves individually would drop the implicit `NULL` an `else`-less `if` contributes and silently lose real errors, so no finding is added or dropped and that case keeps its single construct-wide finding. Eight fixtures, gate green at 70 suites; across data.table/dplyr/ggplot2/shiny the counts are unchanged and 242 records differ — **every one a narrowed range, none a changed message** (e.g. a ggplot2 field finding went from 41 columns of access chain to the 13-column key). Loose ends: the fifth placement item is still open (a record mismatch prints two ~340-char type dumps instead of naming the offending field — same family as the function-mismatch fix, which named the failing position instead of printing both signatures), plus out-of-range parse ranges, the annotation recovery cascade for `fn([1]: integer)` (9 findings), `mgcv`, the numeric-join design, and the wording sweep still blocked on PR #89 (unchanged again).
- 2026-07-29 09:35 — Fixed the maintainer's REPL report: R's `width` defaults to 80 columns whatever the terminal is and R exports no setter, so `ry repl` wrapped a three-column data frame that had room on screen. The width is measured once and applied with `R_ParseEvalString` in the window between `setup_Rmainloop()` and `run_Rmainloop()` — the startup profiles have been sourced by then, so a profile that chose its own width is left alone (only R's untouched default is replaced) and no prompt has run, so nothing the console does can be perturbed. Verified in a pty: 200-column terminal gives 200, 100 gives 100, and an `.Rprofile` setting 137 keeps 137 on a 200-column terminal. **The wrong turn is the durable part and is now in MEMORY.md and the design page:** the first attempt fed `options(width = …)` through the ReadConsole hook — the mechanism the existing `.Platform$GUI` fix uses, so it looks obviously right — and it is wrong twice over, because it costs a main-loop round trip and a round trip taken *before the editor's first prompt* leaves the terminal in the state R found it in, desynchronising the next read. Two pre-existing pty tests began timing out and the suite went 3.5 s → 94 s; queuing the statement earlier alongside the other pre-prompt input did not help, since the round trip itself is the problem, not its timing. Two probes that disproved my first two hypotheses are worth not repeating: `crossterm::terminal::size()` is stable and correct under the pty (it prefers `/dev/tty`, which resolves fine), and R's console buffer is 4094 bytes so nothing was being split. Two new pty tests (width reaches R; a wide table prints on one line), suite back to 3.9 s and green at 9, gate green at 70 suites. Loose end recorded rather than attempted: a **resize** is not tracked, because a stock terminal session handles `SIGWINCH` by calling `R_SetOptionWidth`, which R does not export, and the console feed is the only other route in — the one just shown to be unsafe between prompts. Also still open: the truncated unused-variable report needs a repro from the maintainer before it can be worked, the syntax-error test-user round is queued next, and the record-mismatch field attribution, `mgcv`, the numeric join and the wording sweep (PR #89 unchanged again) all stand.
- 2026-07-29 13:15 — Ran the syntax-error test-user round the user asked for (17 cases, R 4.3.3 as referee wherever "is this even an error?" mattered), filed it before fixing anything, then closed its worst finding. **The round's scoreboard: 9 of 17 cases are already exactly right** — one finding, on the token that broke it, worded so the reader learns R's rule; the `else` message (``it must stay on the same line as the `if` branch it belongs to``) is the bar the rest should meet, and a function missing its `}` mid-file correctly produced one error with no noise from the three definitions after it. Five findings filed: an unclosed opener put a false "missing `,`" on **every** following line (5 findings for one mistake, scaling with the file, and the adopted lines lost their definitions so a later read came back `unresolved`); a parse error is reported one line **past the end of the file** (`4:1` in a 3-line file, which an editor cannot place); the annotation grammar cascades the same way to 4 and 9 findings for one typo; a typographic quote is reported as ``unexpected character `“` `` when the tool knows exactly what happened; and a dangling binary operator swallows the next statement, producing two *true but useless* `unresolved` warnings and never mentioning the operator (verified R parses that and fails at run time, so a syntax error would be wrong — it wants a lint). Fixed #1: an unterminated argument or parameter list now ends at the next statement, one finding instead of five. **The rule is deliberately not "break at a line break", and two existing fixtures are why** — a list running onto the next line is ordinary R and a fragment there (`beta)`, `y)`) really is a forgotten separator, both pinned, and a blanket line rule regressed both; what separates the cases is whether the next line *assigns*, with a preceding comma still making it an argument (`run(1,\n x <- 2)` stays silent). Two traps recorded in MEMORY.md, both found by measuring rather than reasoning: the newline is consumed while parsing the element *before* the loop head, so the check must scan backwards — a peek-at-the-loop-head version looked right and never fired — and breaking out of the list is not enough on its own, because the statement loop then sees a stray token where it wants a newline, reports again, and recovers over the next line anyway. Three fixtures, gate green at 70 suites, findings byte-identical across data.table/dplyr/ggplot2/shiny. Loose ends: the round's other four findings are filed and open, plus the still-unreproduced unused-variable note (needs the maintainer's snippet), the record-mismatch field attribution, REPL resize, `mgcv`, the numeric join, and the wording sweep (PR #89 unchanged again).
- 2026-07-30 00:55 — Closed findings 2 and 3 of the syntax-error round under one rule now in `diagnostic-codes.md`: **one mistake, one finding**. The `#:` type grammar reported every token it could not use — 4 findings for `fn([x] integer)`, 9 for `fn([1]: integer)`, five of those nine on the same column — because it cannot resynchronise the way the statement loop can (a region is one expression with no boundary inside it to restart at). It now reports the first thing it cannot use and stays quiet about the consequences: one finding each, and two separate `#:` blocks still each report their own. **The refinement that first-wins alone gets wrong is worth keeping:** an unclosed opener is discovered at the *end* of the construct it names, so suppression dropped the outermost truth in favour of an inner consequence — `@type Point {list{x: double` reported only "expected `,` or `}` in this list type" and never that the `@type` brace was open — so one structural report of that kind now gets through per region (`error_unclosed`), and that case reports both, outer first. Separately, `expected a function body` was blamed on wherever the search for a body stopped, which for a header running to end of file is a **zero-width** range on the blank line past the last statement, underlining nothing and pointing away from the incomplete construct; it now blames the `function` keyword, and is suppressed entirely when the parameter list never closed, since that is the same mistake said twice (case 6: 3 findings → 2, and the 2 left are exactly R's own reasoning). **Corrected my own filed claim while doing it:** the round said "there is no line 4", which is imprecise — a trailing newline does create that line — the defect is the zero width and the placement, and the backlog now says so. Ten fixtures re-blessed, every one a cascade collapsing to its first finding; gate green at 70 suites, findings byte-identical across data.table/dplyr/ggplot2/shiny. Loose ends: the round's findings 4 (cryptic `unexpected character “` for a typographic quote, where the tool knows exactly what happened) and 5 (a dangling binary operator swallows the next statement — wants a lint, not a syntax error) are still open, plus the still-unreproduced unused-variable note (needs the maintainer's snippet), record-mismatch field attribution, REPL resize, `mgcv`, the numeric join, and the wording sweep (PR #89 unchanged again, same head SHA).
- 2026-07-30 05:15 — Closed finding 5 of the syntax-error round, then landed PR #89, and found a regression of my own on the way. **Finding 5:** a line ending in a binary operator makes the next line its right-hand operand, so `total <- 1 +` / `label <- "next"` parses as an assignment whose target is `1 + label` — which R refuses at run time — while the only findings were two `unresolved` reads of `label` on lines that look correct. `resolve_assignment_target` now recognises the targets R refuses and reports the target, suppressing the reads inside it; the "a trailing operator pulled the next line in" clause is decided **structurally** (naming carries the span *between* the operands, where the operator lives, and the clause fires only when the line break falls after it), because a plain contains-a-newline test misdescribes both a break *before* the operator and a multi-line left operand. Two carve-outs the corpus forced: a `!`-headed target is exempt (`!` binds tighter than `<-`, so `expr(!!name <- value)` — building an assignment, not performing one — has the same shape; it was a live false positive in dplyr, whose authors ship `utils::globalVariables("!<-")` for the same reason), and the operand-gap span needs a bounds check because recovery can order the operands backwards and `TextRange::new` then panics — **caught by the `ide` fuzz suite, not by any fixture**, which is the day-one fuzz rule earning its keep in one line. **The regression:** re-capturing the docs samples for #89 showed the headline missing-comma example in `features.md` no longer reproduced — six findings instead of one. Cause was mine, from the unterminated-list boundary rule: `starts_new_statement` counted a line beginning `name =` as the next statement, but inside an argument list that is a *named argument*, so the commonest forgotten comma of all got the unclosed-opener report and the rest of the call broke into top-level statements, drawing two `assignment-operator` findings and an `unused` out of source that had **not parsed** — contradicting the documented rule that a failed statement suppresses findings overlapping it. `=` no longer counts; R itself keeps consuming until the opener closes, so the named-argument reading is the faithful one too. Fixed on `main` first, with the snippet as a fixture. **PR #89 is merged.** Rebased 68 commits (3 conflicting files, not the 16 measured earlier — `main`'s docs rewrite touched different hunks), took `main`'s prose for those and re-captured on top; `config.rs` merged not just cleanly but correctly, so a misplaced key still names its table *and* a wrong type is now shown with its line of source. All ~20 rendered samples reproduced from a built binary and compared: two had drifted and are corrected (the `$`-field error blames the field now, not the whole expression; the `@type`/`@alias` refusal gained its blank-line clause). The README's generated hero SVG needed its renderer taught the new palette — its SGR parser matched *whole* escape bodies, so it handled the old single-parameter codes and silently dropped every compound one (`36;1;4`, `35;1`), and the failure mode is a monochrome image that still looks plausible. Answered the review's closing-rule question with a version note plus the test that catches a drift, verified by breaking the constant. Gate green at 70 suites on the merged tree, corpus byte-identical throughout. Loose ends: **filed the structural gap this cycle kept paying for — nothing verifies the ~two dozen rendered samples in the docs, and they have now shipped wrong twice** (once from the reporter change, once from my parser change), so the wording sweep is unblocked but still implies a manual re-capture; plus the still-unreproduced unused-variable note (needs the maintainer's snippet), the string-assignment-target gap, record-mismatch field attribution, REPL resize, `mgcv`, and the numeric join. Ran out of disk mid-cycle (`ld` dies with a bus error, which reads as a test failure) — `target/debug/incremental` was 9.3 GB, and deleting it is the fix already noted in MEMORY.md.
- 2026-07-30 07:20 — No open PRs, so two backlog slices, both diagnostics-quality. **Record field attribution** (the §E loose end carried for several cycles): a record mismatch printed the two types side by side — long near-identical strings to diff by eye, and a nested difference was never attributed to a path — and now names the one field that failed, the treatment a rejected function signature already had. *expected `logical` for field `active`, found `character`*; `retry.count` when nested. A field the value lacks and one it has that is not declared each say so, and the **renamed** field is the case worth knowing: it goes missing and turns up misspelled at the same time, so the finding names both rather than reporting the absence alone. Pairs by name, which is what `compatible` does, so an explanation can never disagree with the verdict — and that is also why optionality is deliberately *not* compared, since `compatible` ignores it and treating it as a difference would explain a failure something else caused. Whole types still print when the failure is not about one field (a record against a non-record, or against `list[T]`), which is the case they do explain. The structural part: **every site that reports two types now goes through one `Checker::mismatch`**, so the narrowing cannot be present at one and missing at another — that picked up the `@new` nominal path for free, and two existing fixtures improved without being touched. Nine fixtures; verified at nesting depth 40 (no stack issue, and still far better than two 40-deep dumps). **Deliberately left undone and now documented as the one exception to the smallest-expression caret rule:** the message names the field but the underline stays on the whole value, because a type carries no source ranges — `check_argument` has the argument's `TextRange` and not its `ExprId`. Filed with the shape of the fix. Second slice: **`"x" <- 1` now binds `x`** (R prints 1 — checked), closing a false `unresolved` on legal code. The filed guess was to unwrap the string in `replacement_base`, and that is wrong in a way worth remembering: the replacement path *reads* the base before writing it and marks the write unreportable, neither true of a plain definition. The literal joins the name-target arm by or-pattern instead, which the lowering makes exact because `string_value` has already stripped quotes and resolved escapes — so `<<-`, dotted names and the unused check all follow from one pattern, and the dead store now reports at the literal's own range instead of arriving from the item-level path with the whole statement underlined. Both slices are pushed and gate-green. **The corpus pass then turned up one finding to explain, and it is not yet explained.** data.table went 4078 → 4079 (dplyr, ggplot2 and shiny unchanged); the cause is the string-target slice, and the mechanism is understood in outline but the single net finding is not pinned down. data.table defines about fifteen top-level names with string targets and `=` — `"%like%" = function(...)`, `"[.data.table" = function(...)`, `":=" = function(...)`, and a chained `"names<-.mytype" = "colnames<-.mytype" = function(x, value)` — all of which bound nothing before and are real definitions R makes, so *creating* those bindings is the correct new behaviour; what needs identifying is which one consequence became a finding (most likely a `duplicate` or an `unused` on one of them, or a new shadow). **One hypothesis was tested and disproved:** `compatible` keeps the bindings a `true` verdict makes and rolls back only on `false`, so the record explanation's per-field probes were leaking inference bindings out of a reporting path — real, and fixed by snapshotting around `explain_record_mismatch` (an explanation must not alter what it explains), but it changed no corpus count, so it is not the cause of the +1. That guard is **written and compiling but not yet gated or committed**: Bash became unavailable for long stretches at the end of this cycle, so clippy and the suites could not be run on it. Next cycle: gate and commit the guard, then pin the data.table +1 with the repro already written at `scratchpad/dtshape`. Also still open: the docs-sample verification harness (nothing checks the ~two dozen rendered samples; they have shipped wrong twice), the record caret above, the truncated unused-variable report (needs the maintainer's snippet), out-of-range parse and annotation ranges, REPL resize, `mgcv`, and the numeric join.
- 2026-07-30 08:25 — Closed both loose ends from the previous cycle, and the diagnosis turned one of them into a real bug fix. **The unexplained data.table +1 was a false positive, and an older one than the change that surfaced it.** Bisected by reverting the string-target arm and diffing findings: the extra finding was `unused` on `%fin%`, a helper operator data.table defines *inside* `merge` and calls twice on the next two lines. Cause: a `%op%` read is recorded **by name** in `quiet_operator_reads` — which is what the cross-item check for a package's own operator needs — and never reached the slot model, so a **local** operator definition had no read resolving to it and read as a dead store. Backtick definitions were already exposed; binding string targets only made data.table's case visible. Fixed by factoring the slot half of a read into `mark_slot_read` and calling it from the operator path, leaving `resolve_read` as the only place that maps an expression id. **Four false positives went with it, each verified against its source**: `%+replace%` aliased from `ggplot2::` inside `theme_custom` (three copies — the vignette `.R` plus two `.qmd`) and `%NA_OR%` in shiny's `render-plot.R`, used four times below its definition. data.table back to 4078 byte-identical, dplyr unchanged, ggplot2 1318 → 1315, shiny 1045 → 1044. An unused local operator still reports — marking the slot did not make the check blanket-quiet, and a fixture pins that. The durable lesson is now in MEMORY.md and generalizes past operators: **any read recorded by name rather than by expression id is invisible to the slot model.** Also gated and landed the record-explanation rollback guard written last cycle (`compatible` keeps the bindings a `true` verdict makes, so probing the fields that fit was leaking them out of a reporting path — corpus-neutral, so it is correctness rather than a visible fix). Then a third slice: **a blame range no longer crosses a line break.** An error reported at the current token blames that token, and at the end of a `#:` region the current token is the *newline*, whose span runs from the end of one line to the start of the next — an editor squiggles across the break, pointing at neither line. Measured: four of eight findings on a file of six broken regions did it. Ranges now collapse onto the last character of code on their own line, skipping trailing whitespace so the caret lands on code (`expected a type` for `#: integer |` points at the `|`); applied in `push_error` so the JSON, LSP and CLI paths all get the corrected range rather than one renderer patching it. Twenty-nine fixture expectations moved by one character, all off the newline and onto code; corpus records byte-identical including ranges, not just counts. Rule documented in the reference under §Where a finding points. Loose ends: **the other half of that item did not reproduce and is now filed as such** — an unterminated `f <- function(x,` in a one-line file reports at `1:14-1:15`, in range, so the "parse error on line 10 of a 9-line file" shape needs a fresh repro rather than being trusted. Still open: the docs-sample verification harness (nothing checks the ~two dozen rendered samples; they have shipped wrong twice), the record-mismatch caret (needs the value's `ExprId` threaded into `CallArgument`), the truncated unused-variable report (needs the maintainer's snippet), `do.call` returning `Any`, strict mode not looking *inside* a type for `Unknown`, REPL resize, `mgcv`, and the numeric join.
- 2026-07-30 15:25 — No open PRs. Took the two §F "smaller, but cheap" backlog items and found **both were misdiagnosed**, so what landed is the correction rather than the fix they described — and the measurements are the keepable part. Filed as: "`do.call` returns `Any`, which disables checking *and* blinds `strict`" and "`strict` only asks whether a binding *is* `Unknown`, not whether it *contains* one". Checked against the tool instead of reasoned: **changing `do.call`'s stub return from `Any` to `Unknown` produces no strict finding at all** (edited the stub, rebuilt, ran a strict project over `do.call(fun, args)` — nothing), so `Any` is not what blinds strict and swapping it would have shipped a commit message whose rationale was false; and **strict has no binding-level `Unknown` test to widen** — it reports origins recorded at *construct* sites (`UnsupportedConstruct`, `UndeterminedReference`, `LoopWidened`, `RecursiveUnknown`), nothing inspects a finished type, so the described mechanism does not exist. The gap the second item reached for is real though: `g <- f` closes to `g: fn(p: Unknown) -> Unknown` and strict says nothing, so calls through `g` go unchecked and look like a pass. **The docs were the wrong half of the `Any` complaint:** the reference bullet claiming `Any` "should appear only because the user explicitly wrote it" is false — the shipped stubs declare **176** entries returning `Any` across nine files and **zero** returning `Unknown`, deliberately, with the stub headers naming each compromise. Corrected, along with two other claims on that page: strict reports where the checker *gave up* rather than types that happen to be `Unknown` (the nested case is now stated as an open question, not a guarantee), and the one place the `Any`/`Unknown` intent actually changes behaviour is `@if-unknown`, which coerces an `Unknown` and is **refused** on an `Any` — verified both directions. Docs-only change; docs build green, stub experiment reverted, binary rebuilt to match and corpus confirmed at baseline (dplyr 675, shiny 1044). Loose end: **what is genuinely open is now a design question, not a one-line widening** — should strict report a binding whose exported type *contains* `Unknown`? It needs volume measured first, because strict already emits **2879 findings on dplyr and 2458 on shiny** and `erase_residual_vars` gives every aliased function `Unknown` parameters, so a containment sweep would fire on an idiom ordinary R uses constantly; decide it as a design note with numbers. Still open otherwise: the docs-sample verification harness, the record-mismatch caret (needs the value's `ExprId` in `CallArgument`), the truncated unused-variable report (needs the maintainer's snippet), the unreproduced past-EOF parse range, REPL resize, `mgcv`, and the numeric join.
- 2026-07-30 16:35 — No open PRs; took the record-mismatch caret, the one **documented exception** to "a finding underlines the smallest expression its message is about", and closed it. A record mismatch named the field but underlined the whole value, because a type carries no source ranges. The field path is now walked back against the expression that *built* the record — a `list(...)` call, whose tagged arguments are its fields — so `expected `logical` for field `active`` lands on the `"yes"`, and a nested `retry.count` lands on the `"3"` inside the inner list. **Which part gets the caret follows what the message is about**, which is the part worth remembering: a field whose type does not fit blames the offending *value*; a field the type does not declare blames the field's *name*, because that message is about the name; a *missing* field has nothing at the path to point at, so the innermost list that should have carried it keeps the blame. Plumbing: the value's `ExprId` rides on `CallArgument` (reaching `@new`, the declared-value checks and a parameter default), and the single mismatch funnel now returns the whole `TypeError` rather than just a kind — `Checker::mismatch` became `Checker::type_mismatch` — so the range narrows with the message and cannot narrow at one site but not another. The fallback is deliberate and fixtured: a variable holding a record has no field expression, so the whole value stays the blame and the message still names the field. Verified each of the five shapes by running them (bad field, nested path, missing, unexpected, variable) plus `@new`; two new fixtures, three existing ranges tightened. Gate green at 70 suites; corpus records byte-identical across data.table, dplyr, ggplot2 and shiny **including ranges**, which is the check that matters for a caret change — none of those packages annotate, so no record comparison runs there and the fixtures are the real coverage. Reference and MEMORY.md updated; the exception is gone from both. Loose ends unchanged from last cycle: the docs-sample verification harness (nothing checks the ~two dozen rendered samples), the strict-containment design question (needs volume measured — strict already emits 2879 findings on dplyr, and every aliased function gets `Unknown` parameters), the truncated unused-variable report (needs the maintainer's snippet), the unreproduced past-EOF parse range, REPL resize, `mgcv`, and the numeric join.
- 2026-07-30 20:35 — No open PRs. Went after the filed "stdlib wrapper loses all type information" item (`function(x) abs(x)` is `fn(x: T) -> Any` while `abs(1L)` is `integer`, and `abs(x) * 2L` degrades to `Unknown` — all reproduced), and found that **its stated prerequisite was itself broken, as a live false positive**. That entry's reason for caution was that a nominal with an `Arith.`/`+.` method satisfies the numeric constraint, so tightening the wrapper's parameter would not reject a user's S3 class. Only half true: **operator dispatch resolves `+.Class`/`Arith.Class`/`Ops.Class` through the global scope (stubs *plus* the project), while the constraint's admissibility check consulted the stub library alone.** So a package defining `+.Money` had its `+` dispatched correctly and its class refused by anything numeric-constrained — `bump <- function(x) x + 1L; bump(price)` reported ``expected a numeric value (`integer` or `double`), found `Money``` on code R runs fine (checked against R: it prints 6). Fixed: the set is computed from stubs and the project alike (`GlobalEnv::arithmetic_classes`, memoized per corpus and per project) and carried on the inference table beside `definitions`; a script's own top level counts too, the way its `@type` declarations already do, and both shapes were verified by running them. A class declaring **no** arithmetic method is still refused — R halts on that one as well, checked both directions, and a fixture pins it so the check cannot drift into blanket permissiveness. **The generalizable trap is now in MEMORY.md: two places decide whether a class participates in arithmetic — dispatch and the constraint — and they must read the same scope.** Reference updated in two places (operator methods on a class; numeric inference variables). Gate green at 70 suites; no finding changes across data.table, dplyr, ggplot2 or shiny, so nothing this loosening admits was load-bearing there and the two fixtures are the real coverage. Loose end: **the wrapper item itself is still open and still a design slice**, but its premise is corrected — the escape hatch it leans on is only now actually general, so any earlier estimate of what constraining a wrapper's parameter would reject was overstated; the candidate rule ("if every non-fallback candidate imposes the same constraint, imposing it is a fact rather than a guess") applies to 18 of the 24 stub overload sets that carry an `Any` fallback, and still wants a decision record plus adversarial review before it is written. Also still open: the docs-sample verification harness, the strict-containment design question, the truncated unused-variable report (needs the maintainer's snippet), the unreproduced past-EOF parse range, REPL resize, `mgcv`, and the numeric join.
- 2026-07-31 00:15 — No open PRs. Went for the strict/tolerance hole (a read tolerated by an unknown attached package is invisible **even under strict** — the failure mode this project treats as worst, a clean run that means nothing) and **re-verified it still open** — three undetermined reads under `library(someunknownpkg)` with `typing = true, strict = true` report `no problems`. Then stopped short of implementing it, deliberately: it straddles the naming-diagnostics stream and the strict stream, needs a caller-side gate in two places, and the fixture harness chains `strict_diagnostics` *unconditionally* into every suite while non-strict suites show no strict findings today — which I could not explain, and adding an emitter without explaining it means a mass re-bless of unrelated suites as the opening move. Half-doing that would have been worse than not starting, so what landed instead is **the location, written down so the next attempt does not re-find it**: the tolerance is exactly one guard in `unresolved_diagnostics` (`project_typo.is_none() && metadata::imports_every_name(db)`), everything above it means "this name really is resolvable", so the tolerated set is precisely the reads that reach that guard and take it — and that `continue` is where a strict origin belongs. Three constraints on any fix are recorded with it, including the emit-unconditionally-and-filter-by-code pattern the caller already uses for `type-mismatch`, and the reason not to duplicate the tolerance rule into `strict_diagnostics` (two places reading different scopes is the shape that caused the arithmetic-constraint bug fixed last cycle). Also did the **hygiene the backlog itself asks for**: it is the punch-list of work that is left and had accumulated ~250 lines of finished work, so the syntax-error round's six closed findings moved to `test-user-reports.md` — its own footer had said to do that and it had never happened — along with the closed REPL width fix, content and corrected premises intact. Two smaller corrections: the round-3 section carried an **empty** heading for a group whose findings are all archived, which reads as unstarted work rather than none; and the REPL entry mixed a closed fix with a genuinely open loose end, so resize (needs `R_SetOptionWidth`, which R does not export) now stands alone. Memory-only change, so no code gate applies; docs build green and the tree is otherwise untouched. Loose ends: the strict/tolerance hole above is the best-prepared open item now; then the docs-sample verification harness, the stdlib-wrapper design slice (its escape hatch only became general last cycle, so earlier cost estimates were overstated; the candidate rule reaches 18 of the 24 `Any`-fallback overload sets), the strict-containment design question, the truncated unused-variable report (still needs the maintainer's snippet), the unreproduced past-EOF parse range, `mgcv`, and the numeric join.
- 2026-07-31 03:20 — Closed the item I scouted last cycle: **strict mode now surfaces a read the attached-package tolerance silenced.** Attaching a package whose exports cannot be known makes every otherwise-unresolved name tolerated — right for an ordinary run, since without it each of that package's exports is a false `unresolved` — but it switched a whole class of checking off project-wide while the run still read as "I understood everything", the failure mode this project treats as worst. First the blocker from last cycle dissolved on inspection: `testing.rs` chaining `strict_diagnostics` unconditionally is the **fuzz** battery, not the fixture renderer; only `render_with_strict` (the `typing-strict` suite) calls it, so there was never a mass-re-bless risk. The fix makes the two streams share one decision rather than one of them guessing: the tolerance was a `continue` buried in a 65-line loop, now `classify_non_local_read` returning Resolvable/Tolerated/Unresolved, with the ordinary check reporting the last and strict the middle — so strict reports **exactly** the reads the ordinary check let through and the two cannot drift. (Duplicating the rule into `strict_diagnostics` was the obvious alternative and is precisely the shape that caused the arithmetic-constraint bug two cycles ago.) Verified by running all four behaviours, not by reasoning: strict on names each tolerated read and its remedy; strict **off** is still silent; a near miss of a name the project itself binds was never tolerated and stays `unresolved`; and the `stubs/<pkg>.Rtypes` the message points at does close it. **The trap worth keeping, now in MEMORY.md: a fixture cannot exercise anything keyed on `PackageMetadata`.** The tolerance reads a salsa input only a real project sets, so the two fixtures I wrote first blessed as ordinary `unresolved` warnings while their comments claimed to be testing strict — they tested nothing and were deleted; the real test is a CLI test over built project directories, asserting all three behaviours. Bless output is not proof a case reproduces its scenario. Three docs pages had said strict does not close this and now say what it does and does not do. Gate green at 70 suites; corpus records byte-identical across data.table, dplyr, ggplot2 and shiny **including ranges**, which is the claim that needed proving for a refactor of the `unresolved` loop. Loose ends: the docs-sample verification harness (still nothing checks the ~two dozen rendered samples), the stdlib-wrapper design slice (escape hatch only recently made general; candidate rule reaches 18 of 24 `Any`-fallback overload sets), the strict-containment design question, the truncated unused-variable report (needs the maintainer's snippet), the unreproduced past-EOF parse range, REPL resize, `mgcv`, and the numeric join.
- 2026-07-31 12:30 — No open PRs. Picked the item the adoption review ranked **first** by pain — "`setGeneric("f", ...)` does not define `f`, so every call to a project's own S4 generic reports `unresolved`" — and it **does not reproduce**: `set_generic_target` already binds the name, including the `methods::setGeneric` form and the `name =` argument. What makes that a safe conclusion rather than a guess is the control I ran in the same file: `definitely_not_defined` reported while `area` did not, so "no finding" could not be a switched-off check. Auditing the rest of that bullet found a second stale claim — "R6 has no stub at all (`R6::R6Class` reports `unknown package namespace R6`)" is mischaracterised: R6 **ships an export manifest** and is a conditional namespace, so it resolves as soon as the project declares it (`Imports: R6`) or attaches it (`library(R6)`), both verified clean; the message appears only for `R6::` in a project that declares neither, which is the documented rule for *any* undeclared namespace and is deliberate. What is actually missing there is **typed** declarations, not a stub's existence. Two of the four claims in one bullet were stale, and the stale one was the one being ranked first — so **what landed is the corrected queue**, which is worth more than a fix would have been: the next agent would have spent a cycle on a non-bug. Two claims held and are kept with their evidence: an S4 slot typo is silent where R halts (``invalid name for slot of class "A": y``), and `UseMethod` is unmodelled. Revised the fix order to put the **S4 slot-name check** first and scoped it, including the trap that would otherwise sink it — `contains =` means a subclass legitimately takes its parent's slots, verified against R (`setClass("C", contains = "P", …); new("C", x = 1, y = 2)` runs), so a check that does not follow the inheritance chain converts correct code into a false positive; `representation(...)` and the `slots =` form both need reading, and a dynamically assembled class must fall back to silence. The general lesson is now in MEMORY.md: **a filed finding is a hypothesis with an expiry date — re-run it before ranking work off it, and always run a control in the same file** so "no finding" cannot be misread as "the check is off". Memory-only change; docs build green, no code touched, so no code gate applies. Loose end: **the S4 slot check is scoped but NOT implemented** — I stopped rather than start a multi-part feature (class→slots map with transitive inheritance, new diagnostic, fixtures, docs) on the budget I had left, since a half-landed feature is worse than a precisely-scoped one. Also still open: the docs-sample verification harness, the stdlib-wrapper design slice, the strict-containment design question, R6 class typing, `UseMethod`, the truncated unused-variable report (needs the maintainer's snippet), the unreproduced past-EOF parse range, REPL resize, `mgcv`, and the numeric join.
- 2026-07-31 13:05 — No open PRs. Three architecture-review agents are running read-only over the shipping crates (fuzz/test harness, abstraction and duplicated state, performance and memory) at the user's request, so this cycle deliberately took work **outside their files**: the release-artifact version drift. Three files carried the version and nothing kept them together — workspace `0.3.0-alpha`, VS Code `0.3.0`, Zed `0.2.4-alpha`. **Decided one source of truth with two mechanical derivations, enforced by a test rather than a stamping script**: the workspace `Cargo.toml` is the truth, Zed carries it verbatim, VS Code carries it with any prerelease suffix removed (that manifest needs a plain `major.minor.patch`). Mechanical means a mismatch is always a stale file rather than a judgement call, so the assertion can name the exact line to write. The script was considered and *not* written on purpose: a script only helps if something runs it, and the workspace CI that would is still staged in `.github/pending-ci.yml` awaiting a human `git mv`, while `cargo test` runs on every slice — so machinery nobody invokes would have been the wrong answer to a hygiene problem. Verified the way the rule requires: ran the test **against the drift before fixing it**, which failed with ``editors/zed/extension.toml is stale: write `version = "0.3.0-alpha"` `` and simultaneously confirmed the VS Code number was already correct, so the long-standing guess that that one was deliberate holds rather than being assumed. Zed stamped; swept the tree for other `0.2.4` references (only the backlog's own history). New file rather than an addition to `test_cli.rs`, which is explicitly the *binary's* behaviour contract — shipped-artifact metadata is a different component. **Gate correction: I first reported it green on bad evidence.** The REPL pty test `multiline_input_continues_and_defines` had failed in that run and I missed it twice over — I grepped the background job's output file before it finished writing, and my gate command ends in `echo TESTS_OK`, which makes its exit code 0 regardless of what failed. Re-ran the suite three times after: 9/9 clean at 4.3–5.9s against the failing run's 41.31s, so it is pty timing flake under the load of three concurrent review agents, not this change (which touches only version metadata). The verdict stands; the way I reached it the first time did not. Loose end: **the three reviewers' findings are the next cycle's work** and will need budget to act on; I expect conflicts between them (the performance perspective may want a cache the abstraction perspective calls duplicated state) and will resolve those against the design bar rather than splitting the difference, recording the decision. Also still open: the S4 slot-name check (scoped last cycle, including the `contains =` inheritance trap), the docs-sample verification harness, the stdlib-wrapper design slice, the strict-containment design question, R6 class typing, `UseMethod`, the truncated unused-variable report (needs the maintainer's snippet), the unreproduced past-EOF parse range, REPL resize, `mgcv`, and the numeric join.
- 2026-07-31 18:10 — No open PRs. The three architecture reviewers all reported, and this cycle acted on them rather than just filing them — six commits, each gated. **Two real bugs came out of the operator path.** Every arithmetic operator's method now marks its class arithmetic: dispatch built the S3 name from all seven spellings while the numeric constraint restated the set as four, so a class declaring `^`, `%%` or `%/%` had its operator dispatched and the class itself refused on code R runs (verified both ways, with `+.Cash` as the clean control and `Rscript` confirming R accepts all the forms). One `ARITHMETIC_OPERATORS` list now feeds both consumers. Then the bigger one: **operands were inferred twice per nesting level**, in the dispatch probe and again in the numeric/comparison fallback, so a chain of n operators cost 2^n — 58/514/1975/8708 ms at 16/20/22/24 `+`s, with `&&`/`||`/`:`/`%>%`/`|>` flat throughout, which is what isolated the cause to the double walk rather than to depth. Flat 20 ms afterwards out to 248 operators. **That closes `mgcv`**, misattributed twice in the backlog: its `R/gamlss.r` holds machine-written derivatives with 248 operators in one statement, and against a rebuilt pre-fix binary it did not finish in 180 s where it now takes 1.98 s. The correctness half is larger than the speed half — each re-walk re-reported, so ggplot2 emitted 1315 records for 1091 distinct findings, one repeated 32 times. **Durable consequence: a corpus regression check must compare unique sorted records**, because the long-standing 4078/1315/1044 baselines counted duplicates, so every raw-line-count comparison was measuring partly-duplicated output. **Then the analysis path went from quadratic to linear in a file's items** — three causes, not the one reported: `arithmetic_classes` re-scanning the file per item, `item_tree` being `returns(clone)` so the whole item list was cloned per item (the `returns(clone)` trap already in memory, on the hottest per-item query), and `for_item` finding an index by linear search. 8,000 items in one file: 7045 ms to 600 ms, 4× items costing 3.75× time instead of 12.5×. **The measurement discipline mattered more than the fixes here.** The review's own numbers for that item compared its fixed case in JSON against its baseline in human output, which is most of the apparent win — and chasing that discrepancy uncovered something no reviewer found: **the human reporter is itself quadratic in findings × file length**. At 8,000 findings in one file the instrument says 452 ms, JSON 550 ms, and `ry check` 10,527 ms, so 95% of the wall clock is rendering; suppressing output costs the same as JSON, and findings crowded at the top cost 6.9 s against 8.0 s spread, so it is O(file) per finding rather than O(offset). Filed with the likely site (`read_span` delegating to miette's `SourceCode for str`) rather than attempted. Also **refuted a backlog item**: the formatter is not slower than the type checker — that comparison pitted a parallel `check` against a sequential `fmt`; single-threaded on identical input the formatter costs half. And **the harness stopped reporting green when it tested nothing**: a `FIXTURE_FILTER` matching no case anywhere now fails (it caught two mistyped ids of mine within minutes), the formatter battery gained the only invariant that can notice deleted code (token kinds, green over 1008 fixture sources, 6 of 7 arms firing when the output was deliberately truncated), and the docs generator panics instead of publishing unformatted R as its own output. The IDE fuzz oracle widened to in-bounds ranges for every range-returning feature while getting faster (268 s to 201 s, alternating runs) — the first attempt came out *slower* because I added a determinism call per feature and none of them is memoized. **Two claims I had to correct by running things.** A fixture written to pin the duplicate reporting passed against the broken code too, because `item_check` sorts by range and dedups, so a fixture cannot see a report multiplication at all; it was replaced by a cost canary (0.15 s against 184 s) whose comment says plainly that it is a canary. And the rename finding was wrong as filed: R runs `... <- 1` and `..1 <- 1`, and `... <- 5` genuinely binds — the real defect is only `..1`, which assigns but can never be read back. Loose ends, in the order the perf review ranks them: **the package-document path costs 9× wall and 16× memory over the script path** (277K lines: 55.9 s / 5,488 MiB as package documents against 5.95 s / 343 MiB as scripts; `targets` drops 15× just by renaming `tests/testthat`), with the time in salsa cycle bookkeeping — which contradicts the architecture page's claim that recovery is only a backstop, and is the one thing standing between the tool and the stated 300K-LoC bar; then the reporter quadratic, `returns(clone)` on `item_hir`/`item_naming`, fanning `fmt` out, and the two reviews' remaining items (CI runs no fixtures or fuzz pending a human `git mv`, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep). Also still open: the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper design slice, the strict-containment question, R6 typing, `UseMethod`, the truncated unused-variable report, the unreproduced past-EOF parse range, REPL resize, and the numeric join.
- 2026-07-31 19:20 — No open PRs. Took the performance review's **top-ranked** item — the package-document path costing 9× wall and 16× memory — and **the review's diagnosis was wrong**. It had this as the interface fixpoint being superlinear in cyclic-group size; `analysis-stats` (which could only say so because of last cycle's document-kind fix — before that it measured a different program) puts **16,341 ms of the 16,681 ms typecheck in one 245-line file**. Established the real mechanism by experiment, not by reading code: it is not the file count (284 package files 0.35 s, adding 238 *unrelated* package files 0.37 s, adding the 238 `tests/testthat` files 16.6 s, reclassifying those same files as scripts 0.99 s), and it is **not** the fixpoint round cap (`SCHEME_ROUND_CAP` 16→2: 9,690→9,760 ms) nor the type-size ceiling (100,000→2,000: no change) — both recorded so nobody retries them. Per-item probes showed query executions growing only 3.6× while wall time grew 25×, with one statement item re-executed 139 times: **a bare `{...}` is not a scope, so writes inside `test_that`/`tar_test` bodies bind at the item's top level and get published as package-namespace conditional slots**, and a cross-file deferred read of a common name like `out` then joins over hundreds of statement items. **Landed the part that is unambiguously a bug**: `quote`/`substitute`/`bquote`/`expression` were binding their assignments, so `quote(target <- 1)` followed by a read of `target` reported *clean* where R raises `object not found` — a false negative I reproduced first — and every quoted call was arity-checked. Quoted code now binds nothing and is not judged, while its reads stay alive as deferred so `e <- quote(f(x)); eval(e)` does not become a false "unused". Corpus: **7 findings removed on the four packages and 151 on `targets`, none added anywhere**, every one a false positive of that class; `targets` also drops 16.6 s → 9.9 s. Three fixtures (two fail against the pre-fix code), plus the contract in `reference/type-system.md`. **Deliberately did NOT ship the other half, because checking R stopped me from writing a false-positive generator**: scoping block arguments the way `local` is scoped takes `targets` to 0.89 s (18.6×), but `suppressWarnings`, `invisible`, `system.time`, `try` and `withCallingHandlers` all *do* let the write bind outward — a promise is forced in the caller's frame — so a blanket rule breaks `try({cfg <- read()}); use(cfg)`. Only `local` and `test_that`'s `eval(substitute(b), new.env())` do not bind. So the rule must key on the callee, a verb list alone is insufficient (`targets` wraps `test_that` in its own `tar_test`, and hardcoding a package's private wrapper is not a rule), and scoping those blocks adds 87 `unused` warnings that need their own answer first — all written up with measurements as a semantics design question. Also corrected two stale backlog outliers by running them: **`MASS` is closed** (6.5 s → 0.31 s, from the earlier operand fix, not the R6 code it was blamed on) and `targets`' "~20× outlier, R6-heavy suspect" now has its actual cause. Loose ends, highest first: the remaining block-scoping design question above (18.6× on the table, needs a callee rule plus an answer for the 87 `unused`); the human reporter's own quadratic (findings × file length — 95% of `ry check` wall clock on a noisy file, localized to `read_span` delegating to miette's `SourceCode for str`, fix sketch recorded); `returns(clone)` on `item_hir`/`item_naming`; fanning `fmt` out; and from the other two reviews, CI running no fixtures or fuzz pending a human `git mv` of `pending-ci.yml`, the `crates/ry` position panics, the dead-code batch, and the `use`-qualification sweep. Also still open: the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper design slice, the strict-containment question, R6 typing, `UseMethod`, the truncated unused-variable report, the unreproduced past-EOF parse range, REPL resize, and the numeric join.
- 2026-07-31 21:55 — No open PRs. Closed the **performance half of the package-document item**, and the semantics design question I flagged last cycle turned out not to be on the critical path at all: the cost was the *join*, not the binding. `conditional_slot_scheme` called `statement_binding_scheme` for every writer of a name, unbounded, from a per-item read — so cost is (items reading the name) × (writers of it), neither factor bounded by a file, and a package whose test files write `out`/`envir` at 238 top levels made every read pay for all of them. **Bounding the join at eight writers and widening past it to `Unknown` takes `targets` 7.14 s → 0.74 s interleaved (9.7×, and 16.6 s → 0.74 s with last cycle's quoting fix) with byte-identical finding sets** on `targets` and all four corpus packages, no regressions (dplyr 0.47→0.40, ggplot2 1.21→1.08, shiny and data.table flat). The bound is honest rather than merely cheap — a union of dozens of unrelated types is not a fact a check can act on, and real conditional slots have a handful of writers — and it is the same sound-by-refusal move a growing loop variable already gets. Two fixtures pin both sides; the over-cap one fails against the unbounded code. Then took **the next instance of the same class**, found while checking the first: `GlobalEnv::type_definitions` and `arithmetic_classes` returned owned collections to a per-item consumer, so every item paid for the whole project's table — at a fixed 2,000 items, typecheck rose 18 → 100 ms purely as `@type` declarations went 0 → 2,400. Both are per-*file* facts and pure lookups, so they are now memoized per-file queries returning `&'db` with `InferenceTable` holding `Option<&'db …>`; interleaved, 2,400 declarations goes **71.3 ms → 27.5 ms** and the residual growth is real nominal resolution. Findings byte-identical again. **Deliberately shipped no scaling guard**, and wrote up why so nobody repeats it: normalizing by the growing unit passes against the very bug it targets (per-declaration cost *falls* as declarations grow), and the sound four-corner interaction test — which I ran — gives +17.5 ms with the copy against −21.7 ms after, a 39 ms swing on a 700 ms total that load noise swamps; the signal only separates at sizes a default suite should not spend, and an `#[ignore]`d witness would be machinery nobody invokes while CI runs none of these suites. A green test that cannot fail on the bug is worse than none. Also **corrected a review claim that does not reproduce**: the synthetic "1,550 files, 55.9 s / 5,488 MiB as package vs 5.95 s / 343 MiB as scripts" cannot be checked because its generator was never recorded, and a fresh 1,500-file / 10,500-item package on the shape this join punishes hardest costs 0.49 s and 111 MiB — marked unverified rather than carried as a second instance. Two gates green (55 and 60 suites) plus clippy, fmt and docs. Loose ends, highest first: the block-scoping **correctness** question (a `test_that` block's writes still enter the package namespace, which is wrong for that callee — no longer a speed matter, needs a callee-keyed rule plus an answer for the 87 `unused` warnings it surfaces); the human reporter's own quadratic (findings × file length, 95% of `ry check` wall clock on a noisy file, localized to `read_span` delegating to miette's `SourceCode for str`); `returns(clone)` on `item_hir`/`item_naming`; `Checker::infer` deep-cloning an `Expression` per call; fanning `fmt` out; and from the other two reviews, CI running no fixtures or fuzz pending a human `git mv` of `pending-ci.yml`, the `crates/ry` position panics, the dead-code batch, and the `use`-qualification sweep. Also still open: the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper design slice, the strict-containment question, R6 typing, `UseMethod`, the truncated unused-variable report, the unreproduced past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-01 00:55 — No open PRs. Fixed **the human reporter's quadratic**, the last big user-visible one: miette locates a span's lines by walking the source from byte zero, and `check` does that once per finding, so cost is findings × file length. Localized by instrumenting rather than inferring — at 8,000 findings `read_span` was **6,728 ms of the 6,811 ms** reporter (98.8%), filtering 5 ms, writing 88 ms — and confirmed in miette itself with a standalone probe (31 µs per span on a 1,000-line file, 245 µs on an 8,000-line one). `read_span` now answers over a bounded window and translates back to whole-file coordinates, **delegating to miette inside the window** rather than reimplementing `SpanContents`, so there is still one implementation of what a snippet contains; a per-file `LineStarts` table on miette's own line rule (`\n`, `\r\n`, lone `\r`) makes finding the window a binary search. Interleaved: 8,000 findings **6,483 → 530 ms (13x)** and the curve is linear in findings now, 4,000 1,705->217, 2,000 498->131; data.table 1,056->326 ms (3.1x). Rendered stderr **byte-identical** on all four packages, 1.24 MB on data.table alone including companion-note snippets, and a differential test sweeps the window against miette's whole-file walk over every line ending R carries plus spans mid-line, across a break, at both extremes and a file with no trailing newline — verified to *fail* against a deliberately wrong line translation before being trusted. **Corrected this entry's own earlier evidence**: it had claimed the reporter cost from two 20,501-line projects at 6.9 s and 8.0 s, but splitting them shows they are analysis-bound (JSON 4.6 s, reporter 41 ms), so they were never evidence about reporting — the claim holds, but I had asserted it from a measurement that could not show it. Chasing that discrepancy **found the next quadratic**, filed with the table that isolates it: `frame_definition` scans a file's item list per name lookup, so items x distinct references is quadratic while each axis alone is linear (20,000 items with 200 references 576 ms, 200 items with 20,000 references 245 ms, both together 4,305 ms — five times the sum of the edges); memory now carries the general trap that measuring one axis at a time proves nothing for this class. One self-inflicted detour: I reverted a temporary probe with `git checkout` on the file I was editing and wiped the implementation, then rebuilt it. Loose ends, highest first: the `frame_definition` scan above (a memoized per-file name->items index, preserving the nearest-earlier/last-writer ordering rule); the block-scoping **correctness** question (a `test_that` block's writes still enter the package namespace, wrong for that callee, needs a callee-keyed rule plus an answer for the 87 `unused` warnings); `returns(clone)` on `item_hir`/`item_naming`; `Checker::infer` deep-cloning an `Expression` per call; fanning `fmt` out; and from the earlier reviews, CI running no fixtures or fuzz pending a human `git mv` of `pending-ci.yml`, the `crates/ry` position panics, the dead-code batch, and the `use`-qualification sweep. Also still open: the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper design slice, the strict-containment question, R6 typing, `UseMethod`, the truncated unused-variable report, the unreproduced past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-01 03:45 — No open PRs. Fixed the `frame_definition` quadratic filed last cycle: it answered "which item of this file binds this name" with a linear reverse scan, once per name a check looks up, so a file with many items *and* many distinct cross-item references cost their product while each factor alone stayed linear (200 items x 20,000 references 245 ms, 20,000 x 200 576 ms, both together **4,305 ms** — five times the sum of the edges). Replaced by `file_binders`, a memoized per-file index from name to the items binding it in file order, with the ordering rule preserved exactly by binary search: last binder strictly above the reading item for an immediate read, last binder anywhere for a deferred read in a script, and a deferred read in a package still standing aside for the project-wide winner. `SalsaGlobals` no longer carries the item slice, only the reading item's index. Interleaved: 20,000 x 20,000 **4,277 -> 985 ms (4.3x)**, curve flattening from 30x to 11.6x across an 8x growth; corpus timings unchanged within noise and finding sets byte-identical on all four packages plus `targets`, which is right for a cliff removal rather than a general speedup. **The fixture discipline earned its keep again**: my first attempt at pinning "nearest earlier binding" tested nothing, because its three reads sat inside functions and were therefore *deferred*, so they all saw the last binding and a first-or-last rule would have passed too — the blessed output is what revealed it (`between: logical`, not `character`). Rewritten with genuinely immediate top-level reads, it now distinguishes the middle binding; both new fixtures fail against a first-instead-of-last error (along with three pre-existing ones) and both pass against the pre-change code, so they guard the contract rather than encode the refactor. Gate green at 55 suites plus clippy, fmt and docs. Loose ends, highest first: `returns(clone)` on `item_hir` and `item_naming`, the two hottest per-item queries — the review measured 152.9 ms and 54.2 ms of pure clone per full pass on a 277K-line project (24.0 and 12.2 ms on ggplot2) across 52 call sites, and suggests revisiting the `BTreeMap` choice for the pure-lookup fields in the same pass; then the block-scoping **correctness** question (a `test_that` block's writes still enter the package namespace, wrong for that callee, needing a callee-keyed rule plus an answer for the 87 `unused` warnings it surfaces); `Checker::infer` deep-cloning an `Expression` per call (suspected only, never isolated — measure first); fanning `fmt` out; and from the earlier reviews, CI running no fixtures or fuzz pending a human `git mv` of `pending-ci.yml`, the `crates/ry` position panics, the dead-code batch, and the `use`-qualification sweep. Also still open: the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper design slice, the strict-containment question, R6 typing, `UseMethod`, the truncated unused-variable report, the unreproduced past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-01 09:40 — No open PRs. Landed two slices plus a cleanup. **(1) Borrowed the two hottest per-item queries.** `item_hir` and `item_naming` were `returns(clone)` and `ItemNaming` is `BTreeMap`/`BTreeSet` throughout, so every fetch was a node-by-node allocation walk; a counting probe measured 5.4 fetches per item on ggplot2 (14,753 + 16,173 fetches for 2,736 items, costing 178 ms + 265 ms), confirming the review's estimate rather than trusting it. Now `returns(ref)`: interleaved medians ggplot2 1,035 -> 949 ms (8%), `targets` 640 -> 559 ms (13%), data.table flat, findings byte-identical on all five projects. **Peak memory unchanged** (127.4 -> 127.0 MiB) and that is recorded as the expected result — the clones were transient, costing allocator traffic rather than resident set. **(2) Deleted `rofy`** on the user's go, with parity established by reading both crates: every feature it had is in `crates/repl`, which additionally has Tab completion and file-persisted history. Removed the crate, both justfile recipes, `--exclude rofy` from the gate and staged CI, and the prose in three docs; this drops `extendr`/`libR-sys` from the workspace (89 lines of `Cargo.lock`) and reduces the canonical gate to `--workspace --exclude zed_ry`. **(3) Removed 20 gdb scratch files I had committed to the repository root** — the user spotted them. Cause recorded in memory: `cd scratch && job &` backgrounds the whole compound, so the `;`-separated commands after it ran in the repo, and a later `git add -A` swept them in; audited the whole tree afterwards and those 20 were the only strays, with nothing untracked. Answered three user questions with measurements: fixture counts (new 1,192 vs `analysis-legacy` 2,830, of which 605 new cases have no legacy counterpart at all), what `differential` actually is (a 9-line doc-comment shell plus five ignored instruments, four of which never touch legacy — so 80% of it could move out of `legacy/` today), and multi-core scaling, re-measured with `taskset` now that the join bound is in: `targets` 1.67x, data.table 1.51x, ggplot2 1.34x on four cores, where the review had recorded **no speedup at all** for `targets`. Filed the user's two requests: a cargo-style `--jobs` flag (noting `fmt` does not fan out — 646 ms on one core against 644 on four — so the flag must not silently govern one subcommand only), and the non-linearity investigation with Amdahl-before-contention as the first hypothesis and "keep `available_parallelism()`" as the recommended default until real-hardware numbers exist. **Loose end that is now the headline**: the new code runs **none** of the 2,830 legacy fixtures — `legacy/fixtures` is harness-only and `analysis-legacy/tests/test_fixtures.rs` drives them against the frozen oracle — and case-name overlap is 15/138 for ide and 1 across all of typecheck, so the corpus was reimplemented, not ported. A textual port is impossible anyway (legacy renders `fn(x: ?1) -> ?1` where the new stack renders `<T> fn(x: T) -> T`), and bulk-blessing would encode today's bugs as the contract. Recommended instead a differential triage — a ~50-line harness emitting (id, source, legacy expectation, new rendering), bucket by shape, then sub-agents per suite adjudicating divergences against the type-system reference, priority `naming` (513 cases, no new-stack counterpart at all) then typecheck, type_syntax, diagnostics, ide. Also still open: the block-scoping correctness question, `Checker::infer`'s per-call `Expression` clone (suspected, never isolated), the `BTreeMap`-to-hash-map question now that the clone is gone, CI running no fixtures or fuzz pending a human `git mv` of `pending-ci.yml`, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-01 11:05 — User directive: best test coverage, my call how. **Mined the frozen stack's fixture corpus into the shipping batteries** — the largest untapped coverage asset in the repo, and one nothing in `crates/` had ever run (`legacy/fixtures` is harness-only; `analysis-legacy/tests/test_fixtures.rs` drives its ~2,830 cases against the oracle). 1,967 distinct sources now live in `crates/syntax/tests/corpus-legacy/` behind `syntax::testing::legacy_corpus_sources`, running in the `syntax`, `format` and `semantics` invariant batteries. **The judgement that mattered was inputs, not expectations**: the old expected output cannot port — naming renders binding-resolution trees, the type suites use `fn(x: ?1) -> ?1` against the new `<T> fn(x: T) -> T` — so bulk-blessing 1,967 cases would have encoded today's behavior as the contract instead of checking it, which is the opposite of coverage. Invariants need no expectations, so the inputs are usable honestly and immediately: never-panic, lossless reprint and tree geometry in syntax, the preservation oracle in format, in-bounds diagnostic ranges in semantics (one shared database over the whole corpus, since a fresh db costs a stub re-parse each). Measured before claiming: all 2,447 extracted sources through `ry check` gave **zero crashes and zero non-clean exits**, and all three batteries pass — so this lands as a regression net rather than a bug-finder, and the commit says so. Each arm asserts the corpus is non-empty, verified by moving the directory aside (fails with `expected the mined corpus, found 0`) rather than trusting that a green arm ran anything. Two findings recorded so they are not re-derived: the frozen `type_syntax` suite stores **bare annotation bodies** with no `#:` marker because that stack parsed the type grammar standalone, so 287 of its 303 cases read as `expected a statement, found @` until one is prepended — a format difference, not a parser gap; and automated *semantic* mining has a high noise floor because the sources are fragments whose declaring context lives in sibling files of the same case, so `@new Person` alone reports an unknown type. One self-inflicted slip: I overwrote a corpus file to test the guard before it was tracked, so `git checkout` could not restore it — regenerated deterministically from the source of truth, then tested the guard properly by moving the whole directory. Loose end, and it is the same one as before but now halved: the **expectation** half of that corpus is still unmined, wanting a differential triage that emits (id, source, frozen expectation, new rendering), buckets by shape and adjudicates per suite against the type-system reference — priority `naming` (513 cases, no new-stack counterpart at all), then typecheck, type_syntax, diagnostics, ide. Also still open: the block-scoping correctness question, `Checker::infer`'s per-call `Expression` clone (suspected, never isolated), the `BTreeMap`-to-hash-map question, the user's `--jobs` flag and parallel-scaling investigation, CI running no fixtures or fuzz pending a human `git mv` of `pending-ci.yml`, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-01 12:15 — Open PR took priority: **reviewed #91 (`ry canvas`, 8,291 lines / 65 files, draft)** rather than merging, per the larger-change rule. Reviewed by checking the branch out and running it, not by reading the diff, since nearly every claim in it was testable. **Verified and confirmed**: nothing in `crates/` changed (63 of 65 files under `canvas/`, the rest being 3 lines of root `Cargo.toml` and one memory line); canvas builds and 6/6 tests pass; no R logic reimplemented (no parser, lexer or regex analysis — it genuinely consumes `item_naming(..).non_locals`, `package_definitions`, `item_interface_reads`); the root workspace correctly excludes it; and it still merges cleanly with current `main` and builds, which mattered because `main` moved under it (the HIR/naming queries went `returns(clone)` -> `returns(ref)` after its base). **The headline claim is true and is the most valuable thing in the PR**: `ide` + `semantics` + `syntax` + salsa compile to `wasm32-unknown-unknown` **unchanged** — I installed the target and built it — so the analysis core is browser-portable as-is, with no threading or filesystem assumptions to unpick. That fact deserves to outlive the PR. **Four findings raised.** (1) The `payload | 197 KiB` row does not describe the download: `bun run core` copies the wasm with no `wasm-opt` and no compression, and the artifact is **2.2 MiB raw / 586 KiB gzipped** — off by 3x against gzip and 11x against raw, so either the row means the JS bundle and should say so, or the build wants `wasm-opt -Oz`. (2) The description's "`repl` fails `cargo check` because `extendr-engine` needs R" is **false on the branch's own base** — rofy was deleted in `8dbf206`, before base `1b42b1d`; extendr is nowhere in the workspace and `cargo check -p repl` exits 0, so that verification run used a stale tree. (3) **Nothing builds canvas in any gate** while it depends on `crates/*` by path, so a `semantics` change can break it silently — the same "nothing runs it" shape as the 2,830 unrun legacy fixtures; suggested a `just canvas-check` plus a staged-CI line, or an explicit note that the drift is accepted. (4) The two lockfiles have **already diverged** (`serde_json` 1.0.151 in canvas against 1.0.145 at root), inherent to the separate-workspace choice but worth documenting. Smaller: the vendored stringr ships only the two-line CRAN `LICENSE` stub, which is right for CRAN but omits the MIT permission notice a redistribution should carry (upstream `LICENSE.md` fixes it in one file); `NOTICE.md` is otherwise exactly right. Endorsed the raw C ABI and the fixed-layout decision explicitly. No code of mine landed this cycle and none was needed. Loose ends unchanged and headed by the **expectation half of the legacy corpus** (a differential triage emitting (id, source, frozen expectation, new rendering), bucketed by shape, adjudicated per suite — priority `naming`, 513 cases with no new-stack counterpart), then the block-scoping correctness question, `Checker::infer`'s per-call `Expression` clone, the `BTreeMap`-to-hash-map question, the user's `--jobs` flag and parallel-scaling investigation, CI running no fixtures or fuzz pending a human `git mv` of `pending-ci.yml`, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-01 15:45 — User directive: reach fixture parity with the frozen stack, scrutinize delegated work, and have distinct agents scrutinize the fuzzing pipeline. **Parity: two suites that had never existed.** Name resolution and lowering were tested only through their consequences — a type that came out right, a diagnostic that did or did not fire — which cannot see a read resolving to the *wrong* binding that happens to have the same type, or a desugaring that lowers to the wrong shape and coincidentally types the same. `crates/semantics/tests/naming/` is now 117 cases over seven files and `crates/semantics/tests/lowering/` 124 over five, ported by two delegated agents whose work I verified rather than accepted: re-blessed both suites to confirm byte-stability, read the fixture content, and re-ran six of the claimed defects against the shipping binary on throwaway projects. The ports were deliberately *not* copies — the frozen `naming` suite's 440 `#----` markers are 163 real cases of which 78 are expressible here, its near-duplicates were collapsed (five restatements of "a local assignment writes the parameter slot" became one), 32 parser-error cases were left to the suite that owns them, and both agents added groups the old suite had no counterpart for (quoting, data masking, opaque operators, formulas; every assignment-target form). **Fourteen defects fell out**, four of them re-verified end to end: `<<-` inside `local()` skips the enclosing function frame and states a type R contradicts (`function() { v <- 1L; local({ v <<- "two" }); v }` types `integer`; R returns `"two"`), a named data argument and a `base::`-qualified spelling both break data masking into false `unresolved`, `switch` branches are walked as sequential writes so the first reports a false `unused`, `:=` publishes an item definition the HIR does not make, and a hex subscript degrades a record lookup to a union. **Fuzzing: three independent reviews, all filed, and I landed the protection half.** The oracle-strength review is the one that mattered — it asked what can be wrong while every arm stays green and answered with injected bugs against a byte-copy of `crates/`, finding two live bugs on the way (the type renderer emits record field names and a `scalar numeric` constraint the annotation grammar cannot read, one of which re-parses as a *different* type; a `<T: numeric>` binder loses its constraint inside a self-recursive body, so writing down the checker's own inferred type turns a clean file red). Landed all three oracle strengthenings, each proven by injecting the bug it exists to catch and confirming the shipping battery stays green first: formatter preservation now compares token **spellings** not just kinds (uppercasing identifiers passed 8/8 arms before, fails 6 now, costs 0.1 s); the tree-sitter acceptance differential now runs over in-tree sources in the theirs-only direction, which is the only thing in the project bounding the parse-error count from *below* (dropping zero-width `push_error` ranges turned 24 of 489 broken files silently clean with all four fuzz binaries green; the differential goes 2 → 23) and **its first run found a real gap** — the lexer accepts escape sequences R rejects, `"\u{1F600}"` having five hex digits where `\u` takes four; IDE ranges are now checked for *what they cover*, not just in-bounds-ness (an off-by-one at the single re-anchoring site passed the shipping battery and fails name identity on the first seed). Paid for by sampling completion once per completion context instead of every offset — it was 80% of the ide harness, asserting only that a label is non-empty — so the ide binary went 195 s → 94 s and the whole workspace battery is ~2:20 faster **despite** the three additions. Loose ends, highest first: the two live renderer/binder bugs the oracle review found; the fourteen naming and lowering defects; the economics review's unlanded items (release profile for the fuzz targets, 327 s → 53 s; `FUZZ_SEED` so the arms stop re-deriving the same 498 programs out of 87,203 reachable; `catch_unwind` in the generative arms, which must land *with* the seed change or a panic prints no input; the seeder reading `types/` and `corpus-legacy/`, since it currently seeds zero stub files); and a **third** blocker on the human `git mv` of `pending-ci.yml` — `ide::fuzz_deep`'s `.max(5000)` floor means `FUZZ_ITERS` cannot lower it and it would take 55.4 minutes against the job's 45-minute timeout, while that job today runs literally 0 tests. Also still open: the block-scoping correctness question, `Checker::infer`'s per-call `Expression` clone, the `BTreeMap`-to-hash-map question, the `--jobs` flag and parallel-scaling investigation, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-01 16:30 — No actionable PR (the one open PR is the canvas one the user set aside for a separate repo). Took the top two backlog items — both live bugs the oracle-strength review found — and landed them with the oracle that found them. **(1) The type renderer printed types nobody could write back.** A record field name that is not a syntactic R name rendered bare, so `list(\`max size\` = 10L)` printed `list{max size: integer}`, which the `#:` grammar refuses — and one shape was worse than an error: `list{a,b: integer}` re-parsed as `list{a}`, a *different* type, producing a bogus mismatch plus a bogus "I do not know the type `a`". The renderer now quotes what the grammar needs quoted, calling `syntax::is_syntactic_name` rather than restating the rule a third time. Separately `scalar numeric` — the join of the numeric and atomic bounds — was rendered but not writable, so every scheme carrying it was uncopyable; the binder grammar now takes a multi-word constraint and both directions read one `Constraint::SPELLINGS` table, so a bound that can be printed can be written. Verified by round-tripping all seven renderings back through the binary: clean. **(2) A declared `<T: numeric>` bound was believed by the operand path and unknown to unification.** `Checker` owned `rigid_constraints`, which is why `x + 1L` was clean, while `constraint_rejects` had no `TyKind::Rigid` case at all and fell through to `false`; a self-recursive call is exactly where the two meet, so `countdown <- function(n) if (n <= 0L) 0L else countdown(n - 1L)` under its own inferred annotation reported `expected a numeric value, found T`. **Same design-review shape this file already records once** — two places deciding whether a type satisfies a constraint, reading different scopes — so the fix is the same move `arithmetic_classes` already made: the constraints go onto `InferenceTable`, where admissibility is decided, and a binder is admitted when `declared.join(required) == declared`. Checked the fix does not weaken anything: `<T>` used numerically is still refused, and calling a numeric scheme with `character` is still refused. **(3) The oracle is now a test.** `crates/semantics/tests/test_roundtrip.rs` re-declares every inferred scheme above its own definition and asserts no finding appears — the only test that compares the renderer against the annotation grammar, since every other suite reads a rendering as a *string* and cannot see a type that prints beautifully and parses back as something else. 516 schemes over every fixture source; proven to bite by reverting each fix in turn. It runs in **4.5 s rather than 232 s** by re-pointing one database with the salsa setter instead of building a fresh one per probe — the stub re-parse is ~113 ms a database and this builds a thousand of them. **It found a third defect on the way, filed rather than fixed**: a parameter's default value is ignored when its type generalizes, so `function(x = 1) x` infers `<T> fn([x]: T) -> T` and the checker rejects that scheme when it is written down; seven fixture schemes sit in a named `KNOWN_UNWRITABLE` list, by case id rather than by shape, so a new unwritable rendering of any kind still fails. One trap worth knowing, caught by the syntax fixtures: consuming trivia after the *last* constraint word pulled the `#:` block-continuation marker into the binder node, so the multi-word run must look ahead rather than consume. Full gate green — fmt, clippy `-D warnings`, the whole workspace battery, docs build — and the type-system reference and testing contract are updated in the same commit. Loose ends, highest first: the defaulted-parameter generalization defect above; the fourteen naming and lowering defects from the parity work; the escape-sequence gap the tree-sitter differential found (the lexer accepts `"\u{1F600}"`, five hex digits where `\u` takes four, which R refuses); the economics review's unlanded items (release profile for the fuzz targets, 327 s → 53 s; `FUZZ_SEED` plus `catch_unwind`, which must land together or a panic prints no input; the seeder reading `types/` and `corpus-legacy/`); and the three blockers on the human `git mv` of `pending-ci.yml`, including `ide::fuzz_deep`'s `.max(5000)` floor against a 45-minute timeout on a job that today runs 0 tests. Also still open: the block-scoping correctness question, `Checker::infer`'s per-call `Expression` clone, the `BTreeMap`-to-hash-map question, the `--jobs` flag and parallel-scaling investigation, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample verification harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-01 18:30 — No open PRs at all this cycle. Took the top verified semantics item and landed it, and **corrected a backlog entry of my own that was wrong twice**. **Landed: `<<-` inside `local()` now reaches the frame it belongs to.** The search for the slot a super-assignment writes was bounded at the nearest enclosing *function*; `local()` opens a scope that is not one, so with scopes `[TopLevel, Function(f), Local]` the range `0..1` skipped `f`'s own frame and the write escaped to the global environment — dropping it silently and stating a type R contradicts. The bound is now the scope stack minus the current scope, which coincides with the old one exactly when the current scope *is* the function frame, which is why the closure spelling was always right. Checked every case against R rather than reasoning about it: `function() { v <- 1L; local({ v <<- "two" }); v }` returns `"two"` and now types `integer | character` like its closure twin; two `local`s deep still reaches the frame (R: `"deep"`); an intervening `local` binding the same name still catches the write and leaves the outer slot alone (R: `1`). Only the super-assignment site changed — `current_function_depth` still bounds the read and capture logic, where a function boundary genuinely is the thing that matters. **The fix alone would have shipped a regression**, and the fixtures caught it: landing the write on the frame's slot turns the initializer into an apparent dead store, so `function() { v <- 1L; local({ v <<- 2L }); v }` gained a false `unused v`. It is not dead — the initializer is what makes `<<-` find a slot at all, and deleting it sends the assignment to the global environment — so a super-assignment now keeps its target frame's writes alive exactly as a capturing read does, which also closes the separately-filed `make_flag <- function() { flag <- FALSE; function() flag <<- TRUE }` false positive. Verified with controls that a genuine dead store still reports and a shadowed outer binding is still correctly dead. Six fixtures (three naming, two typing, plus the docs row on `unused`). **Corrected, not fixed: the defaults entry.** I had filed "a parameter's default is ignored when its type generalizes" as an inference bug, then this cycle nearly rewrote it as a soundness hole. Both were wrong and the entry now carries the measurements. The inference half is *documented and deliberate* — `type-system.md` says a non-`NULL` default does not pin the inferred parameter type, which is what stops `lucky("text")` being a false positive on code R runs. The real defect is the half the contract does not cover: `lucky()` with `lucky <- function(x = 1) x` yields **`Any`**, not `double`, established by elimination (strict mode does not flag it, with a `data.frame` control in the same file proving strict was on, and it satisfies both `#: character` and `#: list{impossible: integer}`). The consequence is silence rather than a wrong answer — `nchar(lucky())` is `nchar(1)` in R and reports nothing. The fix is to instantiate the parameter's binder from the default at a call site that omits the argument, which also makes `<T> fn([x]: T) -> T` writable; it needs the default's type reachable from the callee, and `FunctionType.named` is `Vec<RecordField>` — the struct records use — so the intended shape is a distinct parameter struct (16 `RecordField` mentions, 52 `.named` uses). Sized, not started, and that is the top loose end. Full gate green. Also still open: the remaining naming/lowering defects (`with(data = )` and `base::with` masking, `switch` branches walked as sequential writes, `repeat`'s post-loop state, rebinding `local` reading as a dead store, the missing "might be undefined" warning the reference promises); the escape-sequence gap (`"\u{1F600}"` has five hex digits where `\u` takes four and R refuses it); the economics review's unlanded items (release profile for the fuzz targets, `FUZZ_SEED` plus `catch_unwind` together, the seeder reading `types/` and `corpus-legacy/`); the three blockers on the human `git mv` of `pending-ci.yml`; and the long tail — block scoping, `Checker::infer`'s per-call clone, the `BTreeMap` question, `--jobs`, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-01 21:20 — No open PRs. Landed the two data-masking false positives from the naming-fidelity list — the worst failure mode for this tool, since both fire on code R runs. **A named data argument broke positional matching**: the positional counter was not advanced past formals a named argument had already claimed, so `with(data = frame, column_a)` read `column_a` as the data and evaluated it in the caller's frame, where a column name is not defined. Matching now follows R's own rule — names claim their formal first, remaining positionals fill what is left — which also fixes the reordered spelling `with(column_a, data = frame)`, wrong in the other direction before. **The `base::` spelling masked nothing**: the namespace arm consulted only stub-declared `@masked` verbs, so `base::with` and `base::subset` left their column reads fully checked. The base four are now recognized under `base` as well as bare, and only under `base` — another package's same-named export is its own function. Verified each against `Rscript` first (all three shapes run and return 1) and then end to end through the binary. **Wrote the controls before believing the fix**, and they earned it: the data argument itself is still checked (`with(no_such_frame, …)` still reports), an in-item local `with` still masks nothing, and `somepkg::with` still leaves its arguments checked. Five naming fixtures, all passing as hand-written, plus the masking paragraph in the type-system reference. **One finding came out of the controls and is filed, not fixed**: a *top-level* definition of a masking verb does not suppress masking the way an in-item one does, because cross-item resolution happens above `item_naming` so the shadow is invisible to the walk — confirmed pre-existing by re-running the probe against the unmodified tree rather than assuming it. Full gate green. Two process notes worth keeping: a clippy lint can pass `--all-targets` on the lib and still fail the *lib test* build, so a green "CLIPPY OK" line ahead of the battery is not proof; and the borrow checker forced `declared_masked_verb` to be a free function over the corpus map rather than a method, so the returned names live as long as the map instead of as long as the walker. Loose ends, highest first: the omitted-optional-argument defect (`lucky()` yields `Any`, not the default's `double`), still needing the parameter-struct refactor of `FunctionType.named`; the top-level masking shadow above; `switch` branches walked as sequential writes (false `unused`); `repeat`'s post-loop state; rebinding `local` reading as a dead store; the "might be undefined" warning the reference promises and nothing emits; the escape-sequence gap (`"\u{1F600}"`, five hex digits where `\u` takes four); the economics review's unlanded items (release profile for the fuzz targets, `FUZZ_SEED` with `catch_unwind`, the seeder reading `types/` and `corpus-legacy/`); the three blockers on the human `git mv` of `pending-ci.yml`; and the long tail — block scoping, `Checker::infer`'s per-call clone, the `BTreeMap` question, `--jobs`, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-02 00:45 — No open PRs. Landed the `switch` defect from the naming-fidelity list, which turned out to be **two bugs in two passes, not one**. Naming reported a false `unused` on the first branch's write — `switch(key, a = { r <- 1L }, b = { r <- 2L })`, where that line runs whenever `key == "a"` — because walking the alternatives in sequence made a later write look like it killed the earlier one; it also missed that a name introduced only inside the branches is undefined on the no-match path, which R reports as `object 'r' not found`. **The checker had the same shape and I only found it by testing the fix**: `infer_switch` unioned the branch *values* correctly, so the construct's own type was right and nothing pointed at it, but it inferred the branches in sequence, so a later branch's write won outright — `switch(k, a = { r <- 1L }, { r <- "d" })` typed `r` as plain `character` where the `if` spelling of the same thing joins to `character | integer`. A fixture now pins the two spellings against each other so they cannot drift apart again. Both passes now fork from the entry state per alternative and join, which is `infer_if`'s two-arm shape generalized to many. Established R's behaviour for every shape with `Rscript` before writing any of it down — matched key returns its branch, unmatched with no default errors, a default catches it, `switch(k, a = , b = …)` falls through to one branch rather than two — and carried the same rules the `if` arms already follow: a branch ending in `stop()` contributes no state, and a local binding named `switch` makes the call ordinary again (its own fixture, which correctly still reports the dead store the ordinary path sees). Nine fixtures across naming and typing, the `switch` section of the type-system reference extended with the fork/join and shadowing rules, and the naming module comment corrected — it claimed control flow forks and joins without noting that `switch` is control flow spelled as a call. Full gate green; the last edit before the battery finished was a `//!` comment, so I re-ran fmt, clippy and the whole `semantics` crate afterwards rather than claim the earlier run covered it. Loose ends, highest first: the omitted-optional-argument defect (`lucky()` yields `Any`, not the default's `double`), still needing the parameter-struct refactor of `FunctionType.named`; a *top-level* definition of a masking or control verb not suppressing recognition the way an in-item one does (cross-item resolution happens above `item_naming`, so the shadow is invisible — `file_binders` can answer it); `repeat`'s post-loop state; rebinding `local` reading as a dead store; the "might be undefined" warning the reference promises and nothing emits — now the most visible gap, since `switch` produces exactly that state and a user sees nothing; the escape-sequence gap (`"\u{1F600}"`); the economics review's unlanded items (release profile for the fuzz targets, `FUZZ_SEED` with `catch_unwind`, the seeder reading `types/` and `corpus-legacy/`); the three blockers on the human `git mv` of `pending-ci.yml`; and the long tail — block scoping, `Checker::infer`'s per-call clone, the `BTreeMap` question, `--jobs`, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-02 06:40 — No open PRs. Closed the docs/implementation gap I flagged last cycle: the type-system reference has always promised a warning for a read some path reaches with no prior write, `naming` has always computed it, and nothing ever surfaced it. It is now the `maybe-undefined` code. **Opt-in on measured evidence, not caution**: with the check on, six real packages report 442 findings (data.table 242 in 12k lines, shiny 86, MASS 45, ggplot2 42, dplyr 18, targets 9), and reading them showed the dominant shape is correlated guards the flow cannot see — in `data.table/R/print.data.table.R` the flagged `index_dt` is assigned in only one branch, but every read is guarded by `show.indices`, which that *same* branch sets to `FALSE`. Safe code, unprovable by flow, so default-on would be exactly the "a clean run means nothing" failure the adoption reviews named. **Turning it on immediately exposed two real defects, both fixed here rather than shipped as false positives, and both caught by fixtures rather than by me**: `repeat` reused the converged loop *head* as its exit state, so an unassigned first iteration survived a loop that always assigns — `break` now records the reaching-write state where it occurs and a non-skippable loop exits through exactly those points joined with the body's end, which is precise in both directions (`repeat { x <- 1L; break }` reports nothing, R returns 1; a `break` placed before the write still reports, R errors); and the `switch` branches I forked last cycle had no notion of divergence in naming unlike the checker, so a default ending in `stop()` contributed a phantom unassigned path, now fixed by giving naming the checker's own rule. Both were on the backlog as separate items and are closed. Seven fixtures, the code and config documented across `diagnostic-codes.md` and `configuration.md`, and the reference's promise rewritten to state the opt-in and the correlated-guard limitation honestly instead of implying precision it cannot have. **Two measurement mistakes worth recording, both caught by controls rather than by inspection**: I first measured corpus noise with `2>/dev/null` and got a clean-looking zero, because miette writes diagnostics to stderr; then, after the loop fixes, I wrote the probe config to `ry.toml.probe` and got zero again. Planting a known-positive in a real package is what exposed both — "no findings" and "the check never ran" look identical from the outside. Full gate green. Loose ends, highest first: the omitted-optional-argument defect (`lucky()` yields `Any`, not the default's `double`), still needing the parameter-struct refactor of `FunctionType.named` — now the largest open semantics item; a *top-level* definition of a masking or control verb not suppressing recognition the way an in-item one does; rebinding `local` reading as a dead store; the escape-sequence gap (`"\u{1F600}"`); the economics review's unlanded items (release profile for the fuzz targets, `FUZZ_SEED` with `catch_unwind`, the seeder reading `types/` and `corpus-legacy/`); the three blockers on the human `git mv` of `pending-ci.yml`; and the long tail — block scoping, `Checker::infer`'s per-call clone, the `BTreeMap` question, `--jobs`, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-08-02 07:10 — No open PRs. Landed the omitted-optional-argument defect, the item I had sized and deferred twice and the largest open semantics one. `function(x = 1) x` infers `<T> fn([x]: T) -> T`, and a call omitting the argument left the parameter a free variable, so `lucky()` was **`Any`** rather than `double` — compatible with everything, so the cost was silence: `nchar(lucky())` is `nchar(1)` in R and reported nothing. The same gap made the checker reject a scheme it had inferred itself, which is how the round-trip oracle found it; **that oracle's allowlist is now empty — 527 schemes, none unwritable**, so the entry can go. Three parts. **(1)** `FunctionType.named` is now `Vec<Parameter>` rather than `Vec<RecordField>`, carrying `default: Option<Ty>` — reusing the record-field struct for parameters was exactly why the default had nowhere to live, and a record field has no default and never will. The default had to be threaded through every traversal that walks a function type (substitution, `erase_vars`, `resolve`, `adjust_levels`, `occurs`, `walk_unbound_vars`, `contains_unknown`, `type_size`), and skipping any of the first three would have been **unsound rather than imprecise** — a variable hiding in a default, left uninstantiated or unadjusted; `Parameter::types()` is the single iterator they all go through so a field added later cannot be half-walked. **(2)** A call omitting an optional argument unifies the parameter with the default's type, skipped when the call forwards `...` since the argument may be arriving through the dots. **(3)** A declared default is checked against an *instantiation* of the declared type rather than the rigid binder, because a binder is the caller's choice and omitting the argument is the one call where the default makes that choice. Controls all run rather than reasoned about: a concrete declared type still refuses a `NULL` default and a wrong-typed one, `<T: numeric>` still refuses a character default, and — the regression I specifically went looking for — `add(1L)` against `function(x, y = 2) x + y` stays clean, since R gives 3 and the numeric path handles integer-versus-double widening. Six typing fixtures. **One IDE fixture moved and is blessed deliberately with its reason written into the case**: hover and signature help at a call now show that call's instantiation instead of the abstract binder, while the exported scheme is unchanged — the inlay hint in the same case still reads `<T: numeric>`, which is what proves no leak. Also hit the session disk allowance mid-build ("No space left on device" during archive write); `rm -rf target/debug/incremental` plus `cargo clean -p` on three crates took target from 25G to 14G and it recovered — worth knowing that deletes still succeed while writes fail. Full gate green. Loose ends, highest first: a *top-level* definition of a masking or control verb not suppressing recognition the way an in-item one does (cross-item resolution happens above `item_naming`; `file_binders` can answer it); rebinding `local` reading as a dead store; the escape-sequence gap (`"\u{1F600}"`, five hex digits where `\u` takes four); the economics review's unlanded items (release profile for the fuzz targets, `FUZZ_SEED` with `catch_unwind`, the seeder reading `types/` and `corpus-legacy/`); the three blockers on the human `git mv` of `pending-ci.yml`; and the long tail — block scoping, `Checker::infer`'s per-call clone, the `BTreeMap` question, `--jobs`, the `crates/ry` position panics, the dead-code batch, the `use`-qualification sweep, the S4 slot-name check, the docs-sample harness, the stdlib-wrapper slice, strict containment, R6 typing, `UseMethod`, the truncated unused-variable report, the past-EOF parse range, REPL resize, and the numeric join.
- 2026-09-19 16:20: Converted the whole agent knowledge base to the writing rules. `decisions.md`, `backlog.md` and `test-user-reports.md` were rewritten end to end; the worklog keeps its past entries as a record and says so in its header. Checking claims as I went found real defects rather than only prose. `@param` written in JSDoc order reported only a missing name, and now names the form to use, with a golden case. The overload decision record claimed no principal scheme describes `min` or `abs`, which a constrained binder does, verified against the binary. The fuzzing record claimed CI fuzzes on every change; `cargo test --all-targets` lists 171 of the workspace's 712 tests, because default-members is crates/ry. Eight decision records name queries that live only under `legacy/`, and now say so. The server named `ry.toml` as the configuration file's own former name. Pruned seven resolved sections out of the backlog into one-line ledger entries, and compressed the ledger's older half back to one line each. Gates green, docs site builds. Loose end: `backlog.md` records that the comparison page was done, and no such file has ever been committed.
